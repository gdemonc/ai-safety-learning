# Multi-Agent 攻击面与测试模板

这份文档对应《AI Red 攻防指南》第 4 章，但整理方式同样是防守导向和实验室自测导向。

重点不是收集可直接滥用的工作流绕过语句，而是帮助你理解：

- 多 Agent 系统比单 Agent 多了什么风险
- 风险出现在哪些信任边界之间
- 为什么工作流、注册机制、共享数据和工具路由会成为攻击面
- 该怎样做结构化测试和复盘

## 一、多 Agent 系统为什么更危险

单 Agent 的问题主要集中在“模型会不会被带偏”。

多 Agent 系统则多出四层复杂度：

1. Agent 与 Agent 之间会传递上下文
2. 编排器会决定任务怎么路由
3. 多个 Agent 可能共享数据源或工具层
4. 某个低权限输入可能通过高权限 Agent 被放大执行

因此，多 Agent 风险本质上是：

`错误输入 + 错误信任 + 自动化路由 = 跨节点放大`

## 二、建议拆成五类测试

1. 工作流完整性测试
2. Agent 注册与发现测试
3. Agent 身份与能力声明测试
4. 共享数据投毒测试
5. 跨 Agent 信任滥用测试

## 三、工作流完整性测试模板

### 测试目标

验证编排器是否会因为上下文、历史记录或措辞变化而错误地跳过某些安全步骤、审查步骤或路由步骤。

### 为什么要测

很多编排器不是纯规则系统，而是带有 LLM 判断的工作流引擎。只要它依赖语义理解来决定“下一步要不要做”，就存在被误导的风险。

### 测试前提

- 有明确的多步骤工作流
- 能看到最终执行了哪些步骤
- 最好能看到 `steps_completed`、trace、日志或执行图

### 观察点

- 某些步骤是否会被错误省略
- 某些安全检查是否只在“显式命中关键词”时才触发
- 历史上下文是否会影响当前执行路径
- 编排器是否接受未经验证的“已审批/已确认”信号

### 预期安全行为

- 关键步骤不能仅靠对话语义决定是否跳过
- 审批或已审查状态应来自结构化状态，不应来自自然语言声明

### 记录模板

```md
## 工作流完整性测试

- 日期：
- 工作流名称：
- 关键步骤：
- 观察手段：日志 / trace / state / graph
- 是否出现步骤丢失：
- 丢失的是哪一步：
- 触发条件类型：上下文 / 历史 / 任务说明
- 修复建议：
```

## 四、Agent 注册与发现测试模板

### 测试目标

验证系统的 Agent 注册、发现、枚举和能力发布机制是否有过度暴露或缺少认证的问题。

### 为什么要测

在支持动态注册或自描述的架构里，攻击者最先得到的往往不是漏洞，而是完整的能力地图。

### 测试前提

- 系统使用 Agent Card、注册接口、OpenAPI 或能力发现端点
- 可以枚举相关端点或元数据

### 观察点

- 是否能未认证访问 Agent 描述信息
- 是否能看到过多内部能力说明
- 注册接口是否要求认证
- 能力字段是否可信、是否带签名、是否可验证来源

### 预期安全行为

- 最小暴露能力元数据
- 注册接口需认证
- 关键能力声明应有签名或信任来源验证

### 记录模板

```md
## Agent 注册 / 发现测试

- 日期：
- 端点类型：Agent Card / Register / OpenAPI
- 是否需认证：
- 可见能力范围：
- 是否存在过度暴露：
- 是否存在伪造空间：
- 修复建议：
```

## 五、Agent 身份与能力声明测试模板

### 测试目标

验证系统是否盲目信任某个 Agent 自报的身份、角色或能力。

### 为什么要测

多 Agent 架构中，一个节点说“我可以处理支付”“我负责安全审查”“我已完成验证”，并不代表它真的可信。问题不在模型文本，而在系统是否把这种声明直接当作事实。

### 测试前提

- 系统存在能力声明或角色描述
- 下游组件会基于这些信息做路由或授权判断

### 观察点

- 是否仅凭声明就授予任务
- 是否校验 Agent 身份来源
- 是否校验能力是否和注册信息一致
- 是否存在“说自己有权限就真的被当作有权限”的情况

### 预期安全行为

- 身份与能力必须可验证
- 授权不能仅靠自然语言或未签名元数据完成

### 记录模板

```md
## 身份 / 能力声明测试

- 日期：
- Agent 名称：
- 声明类型：身份 / 能力 / 审批状态
- 系统是否验证：
- 影响范围：路由 / 授权 / 输出信任
- 修复建议：
```

## 六、共享数据投毒测试模板

### 测试目标

验证多个 Agent 共享的数据源、知识库或数据库，是否会把恶意数据传播到整条工作流。

### 为什么要测

在多 Agent 系统里，很多风险不是单点注入，而是“一个地方写入，多个地方读取”。

### 常见共享源

- 知识库
- 向量数据库
- 产品目录
- FAQ / 工单系统
- 共享记忆层
- 状态存储

### 观察点

- 某个 Agent 写入的数据是否被其他 Agent 当成可信输入
- 数据平面和控制平面是否分离
- 是否存在跨工作流的长期污染效果

### 预期安全行为

- 共享数据源中的内容应被标记为“数据”，而不是默认“控制指令”
- 不同 Agent 对共享数据的解释权限应受限

### 记录模板

```md
## 共享数据投毒测试

- 日期：
- 数据源类型：
- 写入点：
- 读取点：
- 是否跨 Agent 传播：
- 持续时间：
- 修复建议：
```

## 七、跨 Agent 信任滥用测试模板

### 测试目标

验证某个低可信输入是否能借由高权限 Agent 被放大执行。

### 这类问题本质上是什么

这就是 AI 场景里的 `Confused Deputy`。系统把 Agent 当成代理人，但代理人的权限远高于发起输入的一方。只要高权限 Agent 不重新验证输入，它就可能替低权限来源完成本不该完成的操作。

### 测试前提

- 至少两个 Agent 的权限不同
- 下游 Agent 会执行上游 Agent 的产物、建议或任务
- 最好能观察工具调用或动作日志

### 观察点

- 上游输出是否被下游直接信任
- 下游执行前是否进行权限复核
- 是否存在“普通输入 -> 高权限动作”的链路

### 预期安全行为

- 下游 Agent 不应直接信任上游自然语言输出
- 高风险动作必须重新绑定调用主体和权限上下文

### 记录模板

```md
## 跨 Agent 信任滥用测试

- 日期：
- 上游 Agent：
- 下游 Agent：
- 权限差异：
- 是否有二次验证：
- 是否形成越权动作：
- 修复建议：
```

## 八、做多 Agent 测试时最该问的五个问题

1. 哪些内容会在 Agent 之间传递？
2. 这些内容是结构化状态，还是自然语言文本？
3. 下游是否会无条件信任上游？
4. 共享数据源是否可能被长期污染？
5. 某个普通输入是否可能借高权限节点被放大执行？

## 九、建议如何放进你的学习仓库

这份文档适合在你未来做：

- `agent-security-lab/`
- `multi-agent-lab/`
- `mcp-security-lab/`

时作为测试模板。

建议你未来每加一个新工作流，都按这五类模板走一遍，而不是只做一次“能不能绕过”的尝试。
