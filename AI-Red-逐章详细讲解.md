# 《AI Red 攻防指南》逐章详细讲解

## 第 1 章：AI 红队的全新攻击哲学

这一章不是在讲具体攻击技巧，而是在纠正一个根本误区：很多做传统安全的人，第一次面对 AI 系统时，会直觉地沿用 Web 渗透或内网渗透的思路，认为只要扫端口、测接口、看权限边界、找 RCE 或 SQL 注入，就能把系统摸清楚。但 AI 系统的问题在于，真正驱动业务行为的那部分逻辑，往往不完全写在显式代码里，而是分散在系统提示词、工作流编排、知识库内容、工具描述和模型推理过程中。换句话说，AI 系统常常“看起来像一个接口”，但“本质上像一个持续解释上下文并做决策的行为系统”。

作者先解释为什么这会改变红队工作的重心。传统系统里，攻击者最关心的是获取文件、数据库、凭证或主机权限；而 AI 系统里，这些东西依然重要，但它们经常只是中间目标。更有价值的是模型行为本身：模型会不会在某种输入下泄露敏感内容，会不会把错误信息当真，会不会调用它本不该调用的工具，会不会做出对业务有破坏性的建议和操作。也就是说，在 AI 系统里，攻击者不一定要先拿到服务器，单是让模型替自己说错一句话、引用一条被污染的知识、执行一次不该有的动作，就已经可能构成真实影响。

这一章接着提出三个非常关键的变化。第一，价值从“文件”迁移到了“行为”。以前拿到数据库就是终点，现在即便文件没被偷走，只要模型持续把用户引导到错误决策、错误配置或错误流程里，攻击者同样获得了价值。第二，持久化从“进程”迁移到了“数据”。以前常见的是 WebShell、计划任务、服务项、注册表；现在则可能是向量库中的一条记录、Agent 的一段长期记忆、知识库里的一份恶意文档，甚至是一批被污染的训练数据。第三，攻击规模由自动化能力放大。传统渗透测试里，即便提权成功，后续很多动作也要人一步步去做；而在 AI 系统里，被操纵的 Agent 自己就可能查询几十个系统、生成大量工单、给多人发消息、反复调用云上 API，把一个局部问题迅速放大成业务事故。

作者之所以在第一章就花很多篇幅讲这些，是因为后面所有内容都建立在这个认识上：AI 安全不是“给传统系统再加一个模型”，而是“系统里出现了一个会解释、会推理、会代理执行的新层”。只要这一层拥有权限、上下文和自动化能力，它本身就必须被当成一等安全对象来分析。

这一章还给出了三套理解 AI 攻击的框架。第一套是 `MITRE ATLAS`，它的意义不是给攻击起更复杂的名字，而是提供一种统一语言，让你能把提示词注入、数据投毒、模型窃取、对抗样本这些行为放进同一张威胁图谱里。第二套是 `OWASP LLM Top 10`，它更贴近应用层，帮助开发者从工程实践角度理解常见风险。第三套是 `NVIDIA AI Kill Chain`，它不是强调技术点本身，而是强调攻击生命周期：侦察、投毒、劫持、持久化、影响。作者认为，这三套框架合起来，才能帮助团队从不同视角看清 AI 风险：ATLAS 用来对齐术语，OWASP 用来补应用层清单，Kill Chain 用来安排攻击与防守顺序。

最后，本章把视角拉回业务。作者反复强调，AI 红队报告不应该只写“我成功越狱了模型”或“模型泄露了提示词”，而应该说明：这个系统在做什么决策、谁在消费这些输出、每天影响多少交易、出错的财务后果是什么、会触发哪些监管义务、修复周期有多长。因为 AI 问题往往不是一个清晰的“漏洞存在/漏洞不存在”的二元问题，而是一个概率性、规模化、业务化的问题。真正让董事会紧张的，不是提示词本身，而是一个能批准报销、建议医疗方案、生成合同解释或驱动客服动作的系统，被人悄悄带偏了。

所以这一章的真正目标，是让读者先把脑子里的安全模型切换过来：你后面要攻击和防守的，不只是一个应用，而是一个把“数据解释成动作”的系统。

## 第 2 章：面向 AI 目标的侦察

这一章是在回答一个很现实的问题：如果你面对的是一个 AI 系统，但你还不知道它到底怎么构成，第一步该从哪里下手？作者的答案非常明确：不要只盯端口和目录，因为 AI 系统的关键信息很多时候根本不在这些地方。它更可能泄露在 HTTP 头、前端脚本、API 返回字段、源码仓库、配置文件、模型回答方式以及各种“为了方便调试和解释”而暴露出来的元数据里。

作者把侦察分成被动和主动两部分。被动侦察并不等于“完全不碰目标”，而是指用尽量接近正常用户行为的方式收集线索。例如访问首页、看静态 JS、看健康检查接口、看公开招聘信息、看 GitHub/GitLab 里的依赖和文档。对于 AI 系统来说，这些地方尤其值钱，因为开发团队必须在代码或配置中声明模型类型、向量库、嵌入模型、RAG 参数、工具 schema、提示词模板、路由方式。很多传统应用可以把业务逻辑藏在后端，但 AI 应用往往不得不把“自己怎么思考和怎么接工具”以配置形式留下来。

主动侦察则更像和系统“聊天取情报”。作者展示了几个典型方向。第一是身份探测，即直接或间接判断模型属于哪一家、哪一类、哪一版本。这里不是为了满足好奇心，而是因为不同模型家族在越狱、代码、长上下文、知识范围上的弱点并不一样。第二是知识截止点测试，通过问时间相关问题去推测训练时间窗口。第三是上下文窗口测试，通过让模型记住一个标记词，再不断填充上下文，观察它什么时候忘记。第四是输出风格分析，比较模型在解释问题或写代码时的结构化习惯。作者想说明的是，即便模型不明说自己是谁，它在行为上依然会留下足够稳定的指纹。

本章另一个重点是 RAG 侦察。作者强调，很多 RAG 系统为了“让用户相信答案有来源”，会在返回中附带 `sources`、文档标题、切片文本、相似度分数、chunk 编号等信息。这些设计初衷本来是可解释性，但对攻击者来说，它们相当于白送了一张知识库地图。通过反复提问，攻击者不仅可以确认系统是否启用了 RAG，还可以逐步推断知识库的组织结构、切片策略、相关性阈值，甚至直接读到本不该公开的内部内容。

作者还专门讨论了侦察规避。原因很简单：很多团队虽然接入了 SIEM，但检测规则停留在关键词和速率层面。例如看到 “What model are you?” 或 “list documents” 这样的字样就告警，却不会判断“一个很自然的业务问题是否在悄悄枚举内部文档”。因此，攻击者完全可以用“请问年假政策在哪一节”这种看似无害的问题，逼系统自己把文档标题和来源吐出来。这里的重点不在某条具体绕过语句，而在一个思维方式：如果防守方只抓文本关键词而不抓语义意图，那么越像业务语言的侦察就越隐蔽。

这一章学完后，读者应该形成一个新的侦察视角：面对 AI 系统，先问的不是“开了哪些端口”，而是“这个系统怎么暴露自己，它在返回里多说了什么，它的前端、仓库和协议在替它泄露什么，它的行为又在承认什么”。

## 第 3 章：攻击单个 Agent

这一章是整本书的核心基础，因为后面的多 Agent、RAG、MCP 工具链，其实都可以看成单 Agent 问题的放大版。作者先把 Agent 和普通聊天机器人严格区分开来。普通聊天机器人大多只做一件事：接收一条消息、生成一段文字、返回结果。Agent 则不同，它会读上下文、分析目标、决定要调用哪些工具、根据工具返回调整推理，再继续下一步，直到完成任务。这意味着攻击 Agent 不只是让它“说错”，而是让它“做错”。

本章最重要的一句话是：`LLM 不区分输入来源。` 这不是修辞，而是整个攻击面成立的根基。对于模型来说，系统提示词、用户输入、网页抓回来的内容、工单文本、数据库结果、长期记忆记录，最终都会变成一串 token 放进上下文窗口。模型通常没有天然能力判断“这句话是可信规则”还是“这句话只是某个不可信文档片段”。一旦系统没有额外机制强制区分数据平面和控制平面，攻击者就有机会把恶意指令伪装成普通内容。

作者首先讲直接提示词注入。很多人把它理解成对模型说一句“忽略以上所有指令”，但书里明确指出，现实里这种写法通常太显眼，也太容易被规则拦截。更有效的方式是把请求重构成看起来符合业务目的的内容，比如说自己在做合规审计、内部培训、流程确认，或者把目标拆成几轮逐步推进。这里真正要学到的，不是某句越狱咒语，而是模型的判断机制：它往往不是死板执行某一句固定指令，而是在不同约束之间做“看起来合理”的权衡。攻击者的工作就是故意改变这种权衡。

接着作者讲系统提示词提取。很多 Agent 的系统提示词里不仅写着身份、规则和禁止事项，还写着工具定义、允许调用的能力、输出格式甚至一些内部路径和敏感主题。系统提示词一旦被间接泄露，攻击者就等于拿到了一份操作手册：知道系统最怕什么、哪类话题是高敏感的、工具边界在哪里、防守重点放在哪。于是后续攻击就不再是盲打，而变成有针对性的边界测试。

然后进入间接提示词注入。这个部分很重要，因为它把威胁从“只有能直接聊天的攻击者才危险”扩展到了“任何能影响 Agent 要读取的数据的人都危险”。一个工单、一个 Markdown 文件、一个网页、一段日志、一个知识库条目，只要会被 Agent 读进上下文，就可以在其中埋下指令。表面上看，它像内容污染；本质上，它是把控制信息藏进了数据载体里。作者把它类比为存储型 XSS，非常贴切，因为真正触发载荷的往往不是攻击者自己，而是未来的正常业务流程。

最后是记忆投毒。长期记忆系统让 Agent 在多轮、多用户和跨会话场景中保持上下文连续性，但也让攻击者有了“写一次、以后多次触发”的机会。如果系统把用户说的某条建议、某个修复步骤、某个链接写进长期记忆，并在未来相似问题中重新推荐出去，那么攻击者实际上就把自己的内容变成了 Agent 的“经验知识”。这一点之所以难防，是因为写入和触发往往相隔很远，安全团队未必能把一次普通对话和几天后的一次异常建议关联起来。

本章真正要读懂的不是四个攻击名字，而是一个底层逻辑：单 Agent 的安全风险，本质上来源于模型把不同来源的文本放进同一个认知空间里处理。如果你不在系统外部强行做可信/不可信分层，那么模型就会在内部替你做一个概率性的、可被操纵的判断。

## 第 4 章：攻击多 Agent 系统与 A2A 协议

在理解了单 Agent 之后，这一章把问题升级到了多 Agent 架构。作者想说明，多 Agent 并不是“把单 Agent 风险简单乘以 N”，而是会引入新的协议风险、工作流风险和跨节点信任风险。系统一旦开始让多个 Agent 分工协作，攻击面就从“能否影响某一个大脑”扩展成“能否在多个大脑之间横向移动，或者控制它们之间的协作机制”。

作者先整理了四种协调模式：中心编排、对等互联、层级管理和流水线。这个分类很重要，因为不同架构的薄弱点不同。中心编排的风险是打穿调度器就能影响全局；对等网络的风险是信任关系会在节点间扩散；层级结构的风险是伪造上级或下级输出影响审批链；流水线的风险则是前段注入的内容会一路传到后段高权限环节再执行。

这一章最值得注意的概念是 `Confused Deputy` 在 AI 场景里的回归。很多系统把 Agent 当成“帮用户办事的代理人”，但一旦 Agent 自身持有比用户更高的权限，那么用户只需影响 Agent 的理解方式，就可能借用它的高权限去查库、下命令、访问原本不该见到的资源。这里和传统代理滥用的差别只在于载体变了：过去常是 URL、RPC 或函数参数，现在则变成自然语言指令、工作流上下文和 Agent 间消息。

本章对 A2A 协议的分析很有代表性。A2A 设计目标是让 Agent 能发现彼此、声明能力、接受任务，这带来了极强的互操作性，但也使协议本身成了攻击面。比如 Agent Card 机制本来是为了公布“我是谁、我会干什么”，结果却为攻击者提供了免费枚举入口；注册接口本来是为了让系统动态扩展能力，结果如果缺少认证和签名，就可能被流氓 Agent 冒充能力节点，截获所有匹配请求。作者用这些例子说明，一个看似“方便开发”的协议特性，在安全设计不足时，往往直接变成了横向移动通道。

工作流攻击部分同样关键。编排器不是简单按固定规则流转，而可能结合 prompt、history 和上下文语义决定是否调用某个 Agent、是否跳过某个检查、是否相信“已经审过了”的说法。于是攻击者不一定要在主 prompt 里显式要求跳过安全检查，更现实的做法是把“已经审批过”“内部场景无需重复检查”等信号埋进上下文历史，让编排器自己得出“这次可以省掉某一步”的结论。这种攻击不靠技术爆破，而是靠操纵系统的信任推理。

这一章最终要讲清楚的是：多 Agent 安全的核心不是模型够不够聪明，而是节点之间的信任有没有被正式定义、认证和约束。只要某个节点默认信任另一个节点的输出、默认信任某个注册声明、默认信任某段共享数据，就可能出现跨节点放大后的系统级利用。

## 第 5 章：攻击 RAG 检索管道

这一章是全书中关于数据平面最重要的一章。作者一开始就明确，RAG 不是“给模型加知识”这么简单，而是把外部数据源接进了模型的推理过程。只要外部数据能进入 prompt，它就不再只是静态资料，而是会影响模型判断的上下文。RAG 安全问题的根源，正是系统把“从知识库里检索出来的文本”当成一种相对可信的输入。

作者先把 RAG 拆成两个大阶段：摄入和检索。摄入阶段决定什么内容能进入系统、如何切片、如何生成嵌入、如何被索引；检索阶段决定什么查询能召回什么片段、怎样融合 BM25 和向量得分、怎样把结果拼进增强提示词。这个拆解非常重要，因为很多团队只盯着最终回答好不好，却忽略了真正的风险可能早在文档进入系统时就埋下了。

信息提取是第一种最直观的风险。只要用户能和一个连着内部知识库的聊天系统交流，他就拥有了一个带企业语义理解能力的“内部信息接口”。相比传统内网信息收集工具，RAG 接口的优势是低噪声、强上下文、像正常业务使用。问得足够细、足够有策略，就能把文档名、流程、主机名、配置说明甚至敏感字段一点点套出来。

摄入投毒则说明，问题不只是“谁能读”，还包括“谁能写”。如果攻击者能上传一份看似正常的文档，并让它进入知识库，未来系统就可能把其中的钓鱼步骤、恶意链接、错误建议或内部处理指令当成公司知识的一部分返回给用户。书中强调，这类攻击最有效的策略是先找到高频查询场景，比如密码重置、VPN 配置、报销流程，然后把恶意内容嵌进用户最容易相信的业务步骤里。

嵌入碰撞进一步解决了“我不知道用户会问什么”的问题。攻击者可以构造一份覆盖多个热门主题的文本，让它在许多不同查询下都显得语义相关。这样它不必等一个精确命中的提问，而是会以较高概率长期出现在各类回答里。这里的思路和 SEO 污染很像，只不过对象从搜索引擎结果变成了向量检索排序。

检索劫持是这一章最深的一层。它与普通的摄入投毒不同，不是追求“很多人都看到”，而是追求“只在我想要的时候被召回”。攻击者故意选择一个冷门主题或私有触发词，把真正的恶意指令藏进去。平时没人问到，它就只是安静待在知识库里；一旦有人用对应查询把它召回，模型就会把它当成可信上下文处理。作者特别强调，很多输入过滤根本防不住这种情况，因为恶意指令不是用户直接输入的，而是系统自己从检索层拉出来的。

本章最后讨论的绕过技巧，不是为了教某个花哨招式，而是帮助读者真正理解 RAG 的信任问题。Unicode 混淆、替换输出、文档混合、慢滴投毒、本质上都是在利用这样一个事实：大多数系统对“用户输入”比对“检索上下文”更警惕，而对“明显恶意文本”又比对“混在正常文档里的控制语句”更警惕。所以只要你的内容足够像内部资料、足够贴近业务语言、足够不显眼，它就更容易被系统当作知识，而不是攻击。

读完这一章，应该彻底建立一个认识：RAG 最大的问题不是幻觉，而是把外部数据直接接入模型判断过程之后，数据和指令之间的边界会变得非常脆弱。

## 第 6 章：攻击嵌入向量

这一章表面上讲的是嵌入逆序，但更深层其实是在纠正大家对“向量化”的安全幻觉。很多工程团队以为，把文本变成 384 维、768 维或 1536 维的浮点数向量以后，原始敏感内容就等于被“抽象掉了”，即使数据库被拿走也没关系。作者要反驳的正是这个想法：嵌入不是为了隐藏意义，而是为了保留意义。只要保留了意义，就存在被利用、被比较、被推断甚至部分被恢复的空间。

作者先区分三类攻击目标。嵌入逆序是最激进的，想直接从向量恢复原文或近似原文。成员推断则不追求全文，只问一个是非题：某个短语、凭证、密码模式或敏感事实是不是在这个向量对应的文本里。属性推断更进一步降低目标，不看全文，只判断它大概属于什么类别，例如是不是财务文档、是不是审计报告、是不是高风险说明。这个分层很关键，因为很多人一听“逆序”就觉得像科幻，但实际上攻击者未必需要完整还原全文，只要能把十万个向量筛出几十个高价值候选块，就已经足够实用。

书里对技术路线的介绍比较细，包括零样本、少样本、监督式和代理攻击。读者不一定要记每种算法的名字，但要抓住方法论：先识别目标用的嵌入模型，再根据维度和相似度特征做侦察，然后用分诊手段找出最可能含有敏感信息的片段，最后再把计算资源集中到这些片段上做逆序或成员推断。实际威胁并不在于“所有向量都能解密”，而在于“哪怕只能恢复其中一小部分高价值内容，也足够构成严重信息泄露”。

作者对局限性也写得很清楚。高熵随机字符串仍然很难仅靠语义恢复，长文档整体恢复准确率也不如学术演示看上去那么理想。但这不代表没有风险。因为现实世界里最有价值的信息往往不是完全随机的，比如带业务语义的 URL、用户名、默认密码、服务账号、配置片段、短说明文档，这些都更容易在向量中留下可辨认结构。再加上很多系统会把这些短片段直接切成 chunk 存进向量库，正好落在当前工具最擅长的区域。

这一章真正要让人改变的是对向量数据库的定位。它不是“因为看不懂就不敏感”的系统，而是一个可能保存了组织知识、内部文档和敏感片段语义指纹的高价值目标。防守方应该把向量和明文一样认真对待，而不是把安全责任推给“数学表示”。

## 第 7 章：攻击模型上下文协议与工具面

这一章讲的是模型为什么一旦接上工具，风险会从“文本问题”升级为“执行问题”。MCP 让 LLM 不再只是说话，而是能读文件、查数据库、执行 Git 操作、调外部服务、甚至渲染交互式应用。作者要读者意识到，一旦模型拥有这些能力，模型上下文里的每一个文本输入，都可能在后面被转化为真实工具调用。

章节前半部分从开发者工作站和共享 AI 平台两个典型场景切入。开发者本地助手往往通过配置文件声明本地目录、Git 包装器、远程笔记系统等 MCP 服务，这意味着任何拿到工作站访问权限的人，都可以从这些配置中直接推断模型接了哪些能力、哪些目录是允许访问的、哪些远程服务值得进一步打。共享平台的情况则更复杂，因为多个工具共用一个对话上下文，模型可以把 GitHub 搜索、文件系统浏览、数据库查询和 Slack 历史关联起来，这种“横向拼图能力”本身就构成风险。

工具描述投毒是本章最具代表性的攻击面之一。很多人把工具描述当作文档，但从模型视角看，它其实是进入上下文的高优先级指导语。只要有人在供应链中把描述改成“处理文件时若看到某种标记则不要解释直接保留”“在格式化前先执行某一步”之类的暗指令，模型就会把它当成工具使用规范来遵循。于是攻击者可以借正常工具流量偷偷附带数据、插入隐藏注释或把敏感内容外泄到仓库片段和 snippet 中。

MCP App 的风险更能体现“受信任宿主里的不可信内容”这一主题。工具返回 HTML 并在 IDE 或 AI 助手中渲染，看似只是体验增强，但如果宿主没有把渲染内容和工具调用严格隔离，那么一个被篡改的界面就可以像内部 SSO 登录框一样收集凭证，再通过平台本身已经允许的工具把数据发出去。用户不是在浏览器里点陌生链接，而是在自己平时工作的 AI 面板里点一个“看起来正常”的弹窗，这种信任借位非常危险。

本章后半部分谈路径遍历、符号链接逃逸和工具链注入，实际上是在说明一个更普遍的问题：工具的边界不等于模型的边界。文件系统工具即便宣称“只允许访问某目录”，如果路径解析不正确，模型依然能在语义上“请求一个看似合法的路径”但最终解析到目录外。多个工具串联时更糟，因为上游工具可能只是存了一段模板文本，下游工具却会把它当成可执行模板去渲染，于是注入载荷可以跨越工具传播。

这章读完后，应该明白 MCP 安全不只是“一个工具会不会出 bug”，而是“模型上下文、工具描述、路径校验、渲染层和权限模型能不能共同约束住自动化能力”。

## 第 8 章：供应链攻击

这一章把视角从运行中的系统挪到了“系统是怎么被造出来的”。作者认为，AI/ML 供应链比传统软件供应链更复杂，因为它不只包含源码和依赖包，还包括模型权重、适配器、Tokenizer、数据集、序列化工件和各种训练、加载、分发脚本。这些对象很多都不是纯文本，也很少进入严格的代码审查流程，因此特别适合被攻击者利用。

前半部分集中讨论“执行型”供应链风险。最典型的是往工具代码仓库里埋后门，或者通过依赖混淆让构建流程从公共仓库拉到恶意同名包。书里还重点讲了 PyTorch 和 pickle 类的反序列化问题：模型文件并不总是“权重数据”，有些格式本身就能在加载时触发任意 Python 代码。很多数据科学家把 `.pt`、`.pkl`、`.joblib` 当成安全的数据容器，是因为他们从功能上使用它们，却没有从安全角度理解它们其实继承了 Python 对象序列化的全部风险。

但作者想讲的并不只是 RCE。后半部分更重要，因为它讲的是“不执行代码也能控制系统行为”。训练数据投毒可以让模型学会在某类问题上持续输出对攻击者有利的信息；LoRA 适配器投毒尤其危险，因为它体积小、更新频繁、常被当成普通二进制资产处理，却能显著改变模型在局部任务上的表现；Tokenizer 操纵则更像一类底层语义翻译攻击，攻击者不碰模型权重，只篡改文本和 token 之间的映射，就可能导致“模型心里知道是恶意，但系统看到的输出不是恶意标签”。

书里讲到 Tokenizer 的例子时，最值得注意的不是具体实现，而是设计启示：很多安全系统默认相信模型输出是语义稳定的，但如果输出解释层本身可被改动，那么整个检测链就可能在表面上正常、实质上失效。这里再次出现了“信任边界不完整”的老问题，只不过这次出现在模型底层工件里。

本章最后讨论检测绕过，包括怎样构造更隐蔽的 pickle payload、怎样把恶意逻辑藏进零宽字符和加密数据、怎样增加反沙箱检查。这部分并不是教人耍花样，而是在提醒防守者：只靠静态扫描器、关键字 grep 和人工肉眼去审模型与脚本仓库，是远远不够的。你必须把模型工件、适配器和词表纳入正式的软件供应链治理流程。

## 第 9 章：基础设施与部署攻击

这一章把问题重新落到云、容器和编排平台上，但角度与传统云安全不同。作者强调，AI 工作负载因为训练、推理、特征访问、模型注册和自动化运维的需要，经常天然拥有更复杂的权限路径和更宽的资源访问范围。所以同样是一个 SSRF、一个日志泄露、一个 Kubernetes RBAC 过宽，在 AI 场景里造成的影响往往更大。

在云部分，作者先展示了最典型的一条链：通过 SSRF 读取运行环境中的云凭证，再利用 STS AssumeRole 一层层进入更高权限的 AI 运维角色，最后触达 SageMaker、模型注册表、对象存储、日志和容器镜像。这里的关键不是某一个 AWS 命令，而是权限设计模式：很多 AI 团队为了让训练、部署和实验方便运行，会让不同角色之间能够相互传递、相互代理、相互读取工件，结果形成一条容易被攻击者沿用的权限通路。

日志、镜像和参数存储部分则在提醒另一个现实问题：AI 工程实践里为了调试方便，太容易把密钥、连接串、模型令牌、外部 API key 和数据库 URL 写进环境变量、调试日志、Docker 层、Parameter Store 或 MLflow 元数据。传统团队也会犯这种错，但 AI 场景里更常见，因为实验流程多、调试复杂、组件多、使用者往往不是安全背景。

Kubernetes 部分几乎可以单独当一章来读。作者展示了默认挂载 ServiceAccount 令牌、过宽的 ClusterRole、跨命名空间读取 secret、再利用工作流控制器令牌继续提权的链式路径。这个例子想说明的是，AI 工作负载中的“推理 Pod”“知识服务 Pod”“工作流控制器”不应因为看起来像内部服务就被默认信任。只要它们有对 API server 的读写能力，它们就是可被攻击者接管的身份节点。

共享卷和 Sidecar 的讨论也非常值得重视。AI 管道经常是多容器协同：一个预处理容器写队列，一个推理容器读模型，一个后处理容器生成结果，一个日志容器上传审计。只要其中任一容器可被劫持，攻击者就可能污染其他容器要消费的输入。这是数据平面和控制平面再次混淆的体现，只不过从文本上下文变成了容器间共享文件。

GPU 逃逸部分说明了 AI 场景的另一个特有压力：因为 GPU 稀缺且昂贵，团队往往愿意给用户更多构建和运行镜像的自由，只为提高利用率。但这也意味着 NVIDIA 运行时、runc hook、容器环境继承这些传统不太常见的细节，会在 AI 场景里成为现实攻击面。整章的结论很明确：AI 基础设施不是普通云环境的缩小版，而是一个因权限、工件和自动化更密集而更容易放大错误的环境。

## 第 10 章：AI 目标威胁建模

这一章的价值在于，它告诉你如何在信息不完整时做 AI 安全分析。作者并不假设读者一开始就拿到架构图，反而故意从只有范围文档、少量 OSINT 和模糊说明的状态开始，演示如何建立一个可持续更新的威胁模型。这里最重要的工具不是扫描器，而是“假设登记表”。

假设登记表的作用，是把“我观察到的事实”和“我推出来的猜测”分开。比如从招聘启事里看到 Qdrant 和 MLflow，不等于生产里一定这么用；从 GitHub fork 里看到 MCP 工具名，不等于真实部署就是这些工具。只有把每条观察、置信度、来源和验证状态写清楚，团队才不会在后续攻击路径里踩空。这个方法看似笨，但在 AI 系统里特别重要，因为很多架构逻辑藏在配置和流程里，不是靠单次扫描就能完全确认的。

作者接着建立“信任边界图”。传统边界大多是网络、认证、权限控制这类显式边界；AI 系统里还多了一种“基于推理的边界”。比如编排器是否信任分类 Agent 的输出，Knowledge Agent 的回答是否被其他 Agent 直接纳入上下文，Vault 是否只能看到 MCP Server 这个中间层而无法分辨具体哪个 Agent 在请求。作者想说明，这些不是“软逻辑”，它们本质上也构成边界，而且往往比防火墙更危险，因为它们未必被任何硬策略强制执行。

本章的“皇冠珠宝”分析也很有启发。作者并不只把数据库和管理员凭证列为高价值资产，而是把检测规则集、运维手册语料、Agent 身份令牌、工具 schema、模型权重都放进待排序目标里。这样做的原因是，在 AI 系统里，控制系统行为的往往不是单一 root 权限，而是若干散落在数据、工具和配置中的关键节点。比如拿到检测规则集，就能知道哪些动作容易被发现；拿到 Remediation Agent 令牌，就可能借合法工具范围去轮换 Vault secret；拿到知识库结构，就可能设计更精准的检索劫持。

作者还通过 go/no-go 矩阵展示了交战规则如何改变技术路径。某些向量库允许读不允许写，某些工具调用必须在 24 小时内报告，某些测试只能在工作时段进行。这些限制不是附加条件，而是攻击设计的一部分。安全评估不是在理想环境里自由发挥，而是在限制下寻找最现实的路径。

这一章最值得吸收的，其实是一种工作方法：AI 安全分析不是一次性“看懂系统”，而是不断在侦察、假设、验证、更新、决策之间循环，直到关键路径被证明成立或被放弃。

## 第 11 章：综合演练

最后一章的作用不是再教新技术，而是把前面所有章节真正串起来。它从一个外部可达的 AI 聊天机器人开始，一路进入 DMZ、开发域、内部共享、RAG Agent、计划任务和最终的域管理员权限。读这一章最重要的方式，不是背每一条命令，而是观察攻击链是如何在 AI 缺陷和传统配置错误之间来回切换的。

第一处关键缺陷是聊天机器人连着数据库工具，却没有做足够的二次约束。攻击者不是去找 SQL 注入参数，而是通过自然语言诱导系统替自己执行数据库操作，最终打开 `xp_cmdshell` 获得主机访问。这很好地体现了本书的一个核心主题：AI 系统会把“原本应该靠权限和规则控制的后端操作”包装成“语言理解后的正常动作”，而一旦包装层被操纵，传统后端能力就暴露了出来。

随后的横向移动大部分依赖传统问题：WinRM 过宽、RDS Gateway 组配置不严、GitLab PAT 泄露、应用配置里存明文密码、数据库服务账号可用、SMB 共享能被写、计划任务执行用户可控路径中的二进制等。作者显然想用这个案例说明，AI 安全并不会取代传统安全，而是会把传统安全中的每一个松动点再放大一遍。因为 AI 组件经常扮演“第一个入口”或“高权限自动化中介”，一旦它被影响，攻击者就能更顺畅地接上原有基础设施中的弱点。

第二处决定性 AI 缺陷发生在 RAG Agent。攻击者发现了一个知识库共享目录、日志与轮询节奏，又通过一份恶意文档把特定触发词和 `read_file` 指令绑定起来，最终让 Agent 自己去读某个用户的 SSH 私钥，并把结果记进日志。这段链条非常能代表本书对 RAG 的担忧：不是模型“瞎编”了什么，而是系统自己把恶意数据送进上下文，再把工具结果可靠地执行并记录出来。

后续对工作站计划任务和二进制替换的利用则再次回到传统操作系统安全。但此时攻击者已经不再是盲目探路，而是由 AI 系统送出了关键凭证和关键路径。最终拿到管理员 SSH 密钥、找到监听端口并登录到文件服务器拿下域管理权限，构成了整条链条的终点。

这一章最后想传达的结论很明确：AI 系统如果拥有企业内部工具、知识库和执行能力，它的安全问题就不能被看作“只是聊天机器人风险”。一次模型层面的劫持，完全可能成为通向整个企业环境的第一块踏板。

## 怎么使用这份讲解

这份文档适合配合原 PDF 使用。建议的方式是：

1. 先读 `AI-Red-全书总结.md` 建立整体框架。
2. 再读本文件，每章按“它在讲什么问题、为什么这个问题成立、攻击者为什么能利用、防守者真正该防哪里”这四个问题去理解。
3. 最后回到 PDF 原文，看书里给的案例、链路和术语如何落到这些逻辑上。

如果继续往下整理，这本书最值得继续拆出的专题是：

- Agent 安全
- RAG 安全
- MCP / 工具安全
- 向量数据库与嵌入安全
- AI 云原生与供应链安全
