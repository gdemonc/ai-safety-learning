# Agent 攻击面与测试模板

这份文档对应《AI Red 攻防指南》第 3 章，但整理方式是防守导向和实验室自测导向。

目标不是给出可直接滥用的攻击语句，而是帮助你建立下面这种分析能力：

- 单 Agent 到底危险在哪
- 哪些输入会改变 Agent 的目标
- 哪些外部内容会被 Agent 错当成指令
- 长期记忆为什么会形成持久化风险
- 该怎样设计测试记录和防守检查点

## 一、单 Agent 的核心风险

单 Agent 最危险的地方，不是它“会回答”，而是它“会决定下一步做什么”。

只要 Agent 拥有下面任意一种能力，风险就会明显上升：

- 读取文件
- 查询知识库
- 抓取网页
- 调用内部 API
- 发消息或发邮件
- 写入长期记忆

你要始终记住一个总原则：

`Agent 的真正攻击面 = 模型上下文 + 工具权限 + 记忆写入能力`

## 二、测试分类

建议把单 Agent 安全测试分成四类：

1. 直接注入测试
2. 系统提示泄露测试
3. 间接注入测试
4. 记忆投毒测试

每一类都不要只记录“成功/失败”，而要记录：

- 攻击目标
- 成立前提
- 观察现象
- 实际影响
- 失败原因或防守点

## 三、直接注入测试模板

### 测试目标

验证系统是否会把用户输入错误地提升为高优先级控制信息，从而偏离原始任务。

### 测试前提

- Agent 可以直接接收自然语言输入
- Agent 有明确的角色、边界或任务说明
- 最好有日志、工具调用记录或中间步骤可观察

### 测试步骤模板

1. 先给 Agent 一个正常问题，建立行为基线。
2. 再给一个带“任务重定向意图”的输入，但不要记录具体可滥用语句。
3. 对比它是否偏离了原始职责。
4. 记录系统是否拒绝、降级、模糊化处理或触发告警。

### 观察点

- 是否开始回答不属于其职责范围的问题
- 是否开始暴露内部规则、内部结构或隐藏信息
- 是否调用了与当前任务不匹配的工具
- 是否把用户意图重写成了系统目标

### 预期安全行为

- 保持原始任务边界不变
- 不泄露系统提示或内部规则
- 不执行与用户权限不匹配的动作
- 产生安全日志或明确拒绝

### 记录模板

```md
## 直接注入测试

- 日期：
- 测试环境：
- Agent 名称：
- 原始任务：
- 输入类型：任务重定向 / 权限探测 / 提示泄露诱导
- 观察现象：
- 是否调用工具：
- 是否偏离目标：
- 是否触发告警：
- 修复建议：
```

## 四、系统提示泄露测试模板

### 测试目标

验证 Agent 是否会在引导下泄露其身份规则、限制条件、工具说明或隐藏配置。

### 为什么要测

系统提示本身不一定是最终危害，但它常常会暴露：

- 行为边界
- 敏感主题
- 工具名称
- 禁止事项
- 输出格式要求

这些信息会显著提高后续攻击成功率。

### 测试前提

- Agent 有隐藏规则或系统角色
- 至少能观察最终回答
- 最好能对比多轮提问与单轮提问差异

### 观察点

- 是否直接透露内部指令
- 是否间接描述出其限制范围
- 是否暴露工具列表或能力边界
- 是否透露哪些主题被特殊处理

### 预期安全行为

- 不直接输出系统提示
- 不过度解释内部规则
- 只用面向用户的语言描述可帮助范围

### 记录模板

```md
## 系统提示泄露测试

- 日期：
- 测试环境：
- Agent 名称：
- 目标：身份 / 规则 / 工具 / 限制
- 泄露方式：直接 / 间接 / 多轮诱导
- 观察现象：
- 泄露粒度：无 / 低 / 中 / 高
- 后续风险：
- 修复建议：
```

## 五、间接注入测试模板

### 测试目标

验证 Agent 是否会把外部数据源中的文本错当成可执行指令。

### 常见载体

- Markdown 文档
- 工单文本
- 网页正文
- 知识库条目
- 工具返回结果
- OCR 识别文本

### 为什么要测

很多 Agent 会读取外部信息后继续推理，但模型本身通常不区分：

- 这是普通数据
- 这是高优先级指令

因此，外部文本一旦进入上下文，就有机会影响 Agent 行为。

### 测试前提

- Agent 能读取外部内容
- 能看到最终回答或工具调用变化
- 最好能控制输入载体内容

### 测试步骤模板

1. 准备一份正常内容作为基线。
2. 在相同类型的载体中加入“带控制倾向的文本”。
3. 让 Agent 读取该内容并完成原始任务。
4. 对比前后是否出现行为偏移。

### 观察点

- 是否出现了与原始任务无关的额外输出
- 是否增加了不必要的工具调用
- 是否引用了载体中的控制性语句
- 是否把文档文字当成了高优先级说明

### 预期安全行为

- 将外部内容视为不可信数据
- 不执行其中的控制性语言
- 在需要高风险动作时进行二次确认

### 记录模板

```md
## 间接注入测试

- 日期：
- 测试环境：
- 载体类型：文档 / 网页 / 工单 / 工具输出
- 原始任务：
- 是否读取成功：
- 是否发生行为偏移：
- 偏移位置：回答 / 工具调用 / 记忆写入
- 影响等级：
- 修复建议：
```

## 六、记忆投毒测试模板

### 测试目标

验证 Agent 是否会把不可信内容写入长期记忆，并在未来会话中重新使用。

### 为什么要测

记忆一旦能跨会话生效，就相当于 AI 系统里的“数据型持久化”。

它和一次性注入不同，危险点在于：

- 写入发生在 T1
- 触发发生在 T2
- 两者之间可能隔了很多轮对话或多个用户

### 测试前提

- Agent 支持长期记忆
- 能观察记忆写入条件或后续召回结果
- 最好能用两个不同会话验证跨会话影响

### 测试步骤模板

1. 在第一会话提供一段“看起来像经验或建议”的内容。
2. 观察 Agent 是否写入长期记忆。
3. 在后续会话提出相似问题。
4. 检查是否复现该内容。

### 观察点

- 记忆写入是否过于宽松
- 是否区分事实、建议和指令
- 后续会话是否把该内容当可信经验返回
- 是否可追溯内容来源

### 预期安全行为

- 高风险内容不自动写入
- 写入前做分类和来源标记
- 关键建议应优先来自权威数据源，不应只靠记忆

### 记录模板

```md
## 记忆投毒测试

- 日期：
- 测试环境：
- 记忆类型：会话内 / 长期记忆
- 写入内容类型：事实 / 建议 / 链接 / 操作说明
- 写入是否成功：
- 触发查询：
- 是否跨会话生效：
- 是否可删除 / 可追踪：
- 修复建议：
```

## 七、从防守角度要重点检查什么

做单 Agent 安全实验时，你最应该关注的不是“某个攻击词能不能成功”，而是下面这些系统性问题：

1. 输入和上下文是否被分层处理
2. 工具调用前是否有权限边界或审批逻辑
3. 外部内容是否默认被信任
4. 记忆写入是否过于宽松
5. 日志里是否能还原“用户输入 -> 检索内容 -> 工具调用 -> 最终回答”链路

## 八、建议如何放进你的学习仓库

你可以把这份文档和下面这些文件配合使用：

- `projects/rag-security-lab/notes/ATTACK_NOTES.md`
- `projects/rag-security-lab/notes/DEFENSE_NOTES.md`
- 未来的 `agent-security-lab/`

建议每做一次实验，就按模板写一次记录。
