# 《AI Red 攻防指南》全书总结

## 这本书在讲什么

这本书把 AI 系统视为一种不同于传统 Web、内网和云服务的新型安全目标。它的核心观点是：AI 系统的风险不只存在于代码漏洞、端口暴露或权限错误，更存在于提示词、知识库、Agent 编排、工具调用、向量数据库、模型工件和部署链路中。

传统安全关注“文件、进程、网络、凭证”，而这本书强调 AI 安全还要关注“行为、上下文、自动化动作和被模型信任的数据”。攻击者不一定需要直接拿到系统权限，只要能够影响模型的输入、检索内容、工具参数或长期记忆，就可能操纵系统替自己行动。

## 全书主线

全书可以概括为五条主线：

1. AI 系统的攻击面主要长在上下文、工具和数据流里。
2. 很多 AI 风险不是传统漏洞，而是信任边界没有被明确建模和强制执行。
3. 传统攻击技术在 AI 系统里并没有消失，而是换了外形。
4. 防御不能只靠提示词护栏或关键词过滤，必须做分层控制。
5. AI 红队工作的重点不是只证明“能打”，而是把技术风险翻译成业务影响。

## 第一部分：AI 红队方法论

第 1 章先重新定义 AI 红队。书中认为，AI 安全的核心变化有三点：

- 价值从文件迁移到行为。攻击者想操纵模型输出、业务决策和自动化动作。
- 持久化从进程迁移到数据。投毒知识库、向量库、记忆和训练数据都可能形成长期影响。
- 扩散从人工迁移到自动化。一个被劫持的 Agent 可以自动调用成百上千次工具。

这一部分还引入三套框架：

- `MITRE ATLAS`：用于给 AI/ML 攻击分类
- `OWASP LLM Top 10`：用于理解应用层风险
- `NVIDIA AI Kill Chain`：用于理解攻击阶段和防御切入点

书中强调，AI 红队报告要回答六类问题：系统在做什么决策、谁消费输出、影响规模有多大、潜在财务损失是多少、涉及哪些监管框架、修复周期会多长。

## 第二部分：侦察与单 Agent 攻击

第 2 章说明，AI 目标侦察不应只停留在端口扫描，而应扩展到：

- HTTP 头、健康检查和公开 JS 配置
- 仓库中的依赖、提示词模板、RAG 配置和工具 schema
- 模型行为指纹，如知识截止点、上下文窗口和回答风格
- RAG 返回中的 `sources`、`chunk_id`、相似度分数
- MCP、A2A 等自描述协议暴露的能力信息

作者的关键判断是：AI 系统大量敏感情报会通过“调试友好性”和“可解释性”泄露出来。

第 3 章是整本书的基础章，讨论单 Agent 的攻击面。核心前提是：

`LLM 不区分输入来源。`

系统提示词、用户输入、工具输出、文档内容、长期记忆进入模型后都只是 token。因此，攻击本质上是把恶意控制信息送进模型上下文。主要方式包括：

- 直接提示词注入
- 系统提示词提取
- 间接提示词注入
- 记忆投毒

书中把它们与传统攻击作类比：

- 直接提示词注入像命令注入
- 间接提示词注入像存储型 XSS
- 记忆投毒像二阶注入或持久化后门

## 第三部分：多 Agent、RAG 与嵌入

第 4 章把视角扩展到多 Agent 系统和 A2A 协议。风险重点从单点控制变成横向移动和信任滥用。主要问题包括：

- 编排器工作流被操纵
- 流氓 Agent 注册
- Agent Card 欺骗
- 共享数据源被投毒

多 Agent 系统重新引入了 `Confused Deputy` 问题：一个高权限 Agent 可能被诱导替低权限输入执行动作。

第 5 章专讲 RAG。书中强调：

`检索上下文经常被模型当作可信内部数据。`

由此引出四类主要攻击：

- 信息提取
- 摄入投毒
- 嵌入碰撞
- 检索劫持 / 上下文注入

其核心风险不是“模型会不会乱说”，而是“知识库内容会不会被当作控制指令”。

第 6 章讨论嵌入向量。作者反对把嵌入当成“不可逆哈希”的看法，认为向量保留了语义，因此可能被用于：

- 嵌入逆序
- 成员推断
- 属性推断

书中的现实判断较谨慎：当前最危险的是短文本、结构化文本、弱随机凭证和高价值片段筛选，而不是对所有长文档完整复原。重点结论是：向量数据库也应被视为敏感资产，而不是天然安全的抽象层。

## 第四部分：MCP、供应链与基础设施

第 7 章讨论 MCP 和工具面。MCP 的问题本质上是：模型一旦有了“手脚”，攻击者就会想办法借用这些手脚。主要攻击面有：

- 工具描述投毒
- MCP App 钓鱼
- 工具过度授权
- 路径遍历和符号链接逃逸
- 工具链攻击

这章最重要的启示是：MCP 工具不是普通插件，而是高权限执行平面。任何描述、渲染、路径、模板或参数边界不清晰的地方，都可能变成高价值入口。

第 8 章转向供应链攻击，覆盖：

- 代码后门
- 依赖混淆
- Pickle / Joblib 反序列化 RCE
- 训练数据投毒
- LoRA 适配器投毒
- Tokenizer 操纵

作者提醒，不要把模型、适配器、词表和数据集当“纯数据”。在 AI 系统里，它们都可能改变系统行为，因此都应进入正式的安全治理流程。

第 9 章讨论云和容器基础设施，主要风险包括：

- SSRF 导致云凭证泄露
- IAM 角色链放大权限
- 日志、镜像和参数存储中的密钥泄露
- 未认证推理端点暴露
- K8s ServiceAccount 过度授权
- Sidecar 与共享卷带来的跨容器风险
- GPU 运行时和容器逃逸问题

这一章说明，AI 基础设施往往因为追求实验效率和 GPU 利用率而扩大了权限边界，一旦失守，影响面通常比普通业务环境更大。

## 第五部分：威胁建模与综合演练

第 10 章给出 AI 目标威胁建模方法。重点不是画一张一次性架构图，而是建立一个持续更新的分析过程，包括：

- 假设登记表
- 信任边界图
- 皇冠珠宝排序
- RoE 约束整理
- go / no-go 决策

作者指出，AI 系统最危险的往往不是显式漏洞，而是从未正式定义的信任：

- 编排器默认信任分类结果
- Agent 默认信任知识库输出
- 工具后端看不到真实请求来源
- 检索内容被当作可信内部材料

第 11 章用综合案例把前面所有思想串起来，展示 AI 缺陷如何与传统配置错误结合，形成从公开入口到域控级别的完整攻击链。最终结论很明确：如果一个 AI 系统能访问后端工具、内部文档和企业工作流，它就必须被当作高权限系统来治理。

## 这本书最重要的结论

把整本书压缩后，最核心的结论有六条：

1. AI 系统真正的攻击面在“上下文、工具、数据和自动化流程”。
2. 提示词、RAG、记忆、MCP、Agent 编排，本质上都是新的信任边界。
3. 传统安全问题在 AI 系统里重新出现，只是载体从参数、文件和接口变成了文档、提示词、工具和向量。
4. 关键词黑名单和表面护栏不够，必须在输入、上下文、工具、数据和部署层同时治理。
5. 向量数据库、模型工件、适配器、词表和知识库都应被视为敏感资产。
6. AI 红队不仅要证明“能攻击”，更要解释“会造成什么业务影响”。

## 学习建议

如果把这本书作为 AI 安全入门到进阶的路线，可以这样看：

- 想先建立整体视角：先读第 1、10 章
- 想理解 Agent 安全：重点读第 3、4、7 章
- 想理解 RAG 风险：重点读第 5、6 章
- 想理解工程落地和平台问题：重点读第 8、9 章
- 想看完整攻击链如何串起来：读第 11 章

如果继续做学习整理，最值得扩展的内容是：

- 术语表
- 每章关键攻击面卡片
- RAG / Agent / MCP / 云原生 AI 安全专题笔记
- 防御视角的检查清单
